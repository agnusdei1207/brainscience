+++
weight = 10
title = "10. 세마포어 & 뮤텍스 (Semaphore & Mutex)"
date = "2026-05-17"
[extra]
categories = "gisulsa-operating-system"
exam = "★ 132회 기출"
+++

# 🎯 핵심 키워드: 세마포어, 뮤텍스, 모니터, 임계구역

> **출제 빈도**: ★★★★★ | **기출**: ★132회

## 1. 정의

**세마포어(Semaphore)**: 정수 값을 가진 동기화 도구. P(Wait) 연산으로 감소, V(Signal) 연산으로 증가. 다중 자원 접근 제어 가능.

**뮤텍스(Mutex)**: 상호배제(Mutual Exclusion)를 위한 이진 잠금. 소유권 개념 존재 (획득한 스레드만 해제 가능).

**모니터(Monitor)**: 고급 언어 수준의 동기화 구조. 조건변수(condition variable)와 함께 사용.

## 2. 출제 포인트

| 포인트 | 내용 | 채점 비중 |
|--------|------|----------|
| 세마포어 정의 | P/V 연산, 이진/계수 세마포어 | 25% |
| 뮤텍스 vs 세마포어 | 소유권, 재귀, 사용 목적 | 35% |
| 모니터 | wait/signal/notify, Java synchronized | 20% |
| 문제 해결 | 생산자-소비자, 독자-저자 | 20% |

## 3. 답안 목차 템플릿

**문제**: "세마포어와 뮤텍스의 개념을 비교하고, 각각의 사용 사례를 설명하시오."

```
I. 임계구역 문제와 동기화 필요성
   - 경쟁 조건 (Race Condition): 실행 순서에 따라 결과 달라짐
   - 임계구역 (Critical Section): 공유 자원 접근 코드 영역
   - 3가지 요구 조건: 상호배제, 진행, 한정 대기

II. 세마포어 (Semaphore)
   
   [이진 세마포어] sem = 1
   P(sem): sem > 0이면 sem--, else 대기
   V(sem): sem++, 대기 중 프로세스 깨움
   
   [계수 세마포어] sem = N (자원 수)
   → N개의 자원 풀 관리 가능
   
   [생산자-소비자 문제]
   empty = N (버퍼 빈 슬롯)
   full = 0  (버퍼 찬 슬롯)
   mutex = 1 (버퍼 접근 상호배제)
   
   생산자: P(empty) → P(mutex) → 생산 → V(mutex) → V(full)
   소비자: P(full)  → P(mutex) → 소비 → V(mutex) → V(empty)

III. 뮤텍스 vs 세마포어
   | 항목       | 뮤텍스              | 세마포어         |
   |-----------|--------------------|--------------------|
   | 소유권     | 있음 (획득 스레드만 해제) | 없음           |
   | 타입       | 이진 (0/1)         | 이진/계수        |
   | 재귀 획득  | 가능 (재귀 뮤텍스)  | 불가 (교착 위험)  |
   | 주 목적    | 상호배제            | 자원 개수 제어   |
   | 우선순위역전| 방지 프로토콜 있음  | 없음             |

IV. 모니터 (Monitor)
   - 객체지향 동기화 추상화
   - 진입 시 자동 잠금, 탈출 시 자동 해제
   - wait(): 조건 불만족 시 대기 큐로
   - signal()/notify(): 대기 중 스레드 하나 깨움
   - Java: synchronized 키워드 = 모니터
```

## 4. 핵심 암기 포인트

- **P = Wait = 감소**, **V = Signal = 증가**
- **뮤텍스 = 소유권 있는 이진 세마포어** (단, 완전히 같지 않음)
- **우선순위 역전**: 뮤텍스는 우선순위 상속으로 해결 (★131회 연계)

## 5. 관련 개념 연결

| 개념 | 연결 포인트 |
|------|------------|
| 교착상태 (★134회) | 세마포어 잘못 사용 시 발생 |
| RTOS 스케줄링 | RTOS에서 우선순위 상속 프로토콜 |
| 생산자-소비자 | 세마포어 대표 적용 사례 |
| eBPF (☆예측) | 커널 동기화 관측 |
