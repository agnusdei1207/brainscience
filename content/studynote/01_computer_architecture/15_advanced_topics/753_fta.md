+++
title = "753. FTA (Fault Tree Analysis)"
date = 2026-03-20
weight = 753
description = "서버 다운이나 미사일 추락 같은 최상위 재앙(Top Event)에서 출발하여, 그 원인이 되는 하위 결함들을 논리 게이트(AND, OR)로 역추적해 나가는 하향식(Top-Down) 신뢰성 분석 기법"
taxonomy =  ""
tags = ["Computer Architecture", "Advanced Topics", "Reliability", "FTA", "System Design"]
[extra]
categories = "studynote-computer-architecture"
+++

## 0. 핵심 인사이트

> **핵심**: 앞서 배운 FMEA가 "이 나사가 빠지면 어떻게 될까?"라고 상상하는 밑에서 위로(Bottom-Up) 올라가는 방식이라면, **FTA**는 반대로 **"서버가 불탔다(Top Event)! 왜 그랬을까?"**라고 위에서 아래로 파고드는 연역적 방식이다.
> **비유**: 살인 사건이 터진 후 범인을 찾는 탐정의 방식입니다. 시체(Top Event)를 보고 사인을 유추한 뒤, 용의자들을 하나씩 좁혀 내려가며 퍼즐을 맞추는 가장 논리적이고 직관적인 추리법입니다.

---

📝 모범 답안

---

## 1. 개요 및 필요성

FMEA는 10만 개의 부품을 다 검사해야 해서 너무 피곤합니다.
반면 FTA는 우리가 가장 두려워하는 **최악의 시나리오 단 1개(Top Event)**를 칠판 맨 위에 적어놓고 시작합니다.

* **Top Event**: "데이터센터의 메인 DB 서버가 완전히 꺼짐"

이 사건이 일어나려면 무슨 조건이 성립해야 할까?
* "전원 공급이 끊기거나(OR) 메인보드가 타버려야지."
* "전원 공급이 끊기려면? 한전 전기도 나가고(AND) 비상 발전기도 고장 나야 해."

이처럼 원인을 파고들며 가장 밑바닥에 있는 근본 원인(Basic Event, 더 이상 쪼갤 수 없는 부품 고장)이 나올 때까지 나뭇가지처럼 그림을 그립니다.

* **의미**: 밑에 있는 원인 중 **단 하나라도** 터지면 위의 사고가 발생함. (취약점)
* **예시**: `서버 다운` = `메인보드 고장` **OR** `CPU 고장`
* **확률 계산**: 더하기와 비슷하게 확률이 팍팍 올라갑니다. (위험함)

---

## 2. 구성요소

| 요소 | 역할 | 핵심 포인트 |
|:---|:---|:---|
| 핵심 대상 | FTA (Fault Tree Analysis)에서 직접 다루는 연산, 자원, 상태, 지표 | 문제 정의와 측정 기준을 먼저 명확히 해야 함 |
| 제어 메커니즘 | 동작을 결정하는 규칙, 신호, 정책, 알고리즘 | 성능과 안정성의 균형을 좌우 |
| 구현 자원 | 회로, 메모리, 인터커넥트, 전력, 소프트웨어 협력 요소 | 병목과 비용의 직접 원인 |
| 결과/출력 | 처리 결과, 상태 변화, 품질 지표, 후속 제어 신호 | 상위 계층의 의사결정 근거 |

FTA 도면의 핵심은 원인들을 이어주는 논리 게이트입니다. 이 게이트가 나중에 **장애 확률(%)**을 계산하는 수학 공식이 됩니다.

* **의미**: 밑에 있는 원인들이 **모두 다 같이** 터져야만 위의 사고가 발생함. (방어막)
* **예시**: `전원 완전 차단` = `한전 정전` **AND** `UPS 배터리 폭발`
* **확률 계산**: 확률이 곱하기로 극단적으로 작아집니다. (매우 안전함)

### FTA 구조도 (ASCII)

```text
               [ 🚨 시스템 완전 셧다운 (Top Event) ]
                                                         │
                    ┌──── ( OR 게이트 ) ────┐ ◀ (둘 중 하나만 터져도 죽음)
                    │                                    │
           [ CPU 폭발 (1%) ]         [ 전원 완전 단절 ]
                                                         │
                                  ┌─── ( AND 게이트 ) ───┐ ◀ (둘 다 터져야만 죽음 = 이중화)
                                  │                      │
                          [ 한전 정전 (5%) ]   [ UPS 고장 (2%) ]
```
*(위 시스템이 셧다운 될 확률 = CPU 폭발(0.01) + (한전 정전(0.05) * UPS 고장(0.02)) = 약 1.1%)*

---

## 3. 구조 및 원리

**핵심 조건**: FTA (Fault Tree Analysis) 적용 시 입력 조건, 내부 상태, 제약 자원이 함께 맞물려야 기대한 동작이 성립한다.

설계 도면을 그려서 FTA를 돌려보면 눈에 보이지 않던 **SPOF (단일 장애점)**가 빨간색으로 툭 튀어나옵니다.

만약 어떤 거대한 재앙 밑을 따라가 봤더니 덜렁 **OR 게이트** 하나에 100만 원짜리 싸구려 스위치 1개가 물려있다면? 엔지니어는 등골이 오싹해집니다. 당장 예산을 뜯어내어 스위치를 2개 사고, 그 자리를 **AND 게이트(이중화)**로 바꿔치기해야 합니다.
이처럼 FTA는 "우리가 돈을 어디다 부어야 서버가 가장 튼튼해질까?"를 경영진에게 수학적 확률로 설득할 수 있는 최고의 아키텍처 검증 보고서입니다.

동작 순서:
1. 입력값, 상태 정보, 제어 조건이 먼저 정의된다.
2. 내부 구성요소가 정해진 규칙에 따라 상호작용하며 핵심 처리가 수행된다.
3. 처리 결과가 출력, 상태 갱신, 후속 제어 신호로 연결되어 다음 단계 동작을 결정한다.

```text
[입력 / 상태 / 제어 조건]
            ↓
     [핵심 처리 로직]
            ↓
    [출력 / 상태 갱신 / 효과]
```

---

## 4. 비교 및 연결

| 관점 | 기본 접근 | 최적화/확장 접근 |
|:---|:---|:---|
| 초점 | 기능을 성립시키는 최소 구조 | 성능·전력·확장성까지 함께 최적화 |
| 장점 | 구현과 이해가 단순함 | 대규모 시스템에서 효율이 높음 |
| 주의점 | 병목과 한계가 빨리 드러남 | 검증·설계 복잡도와 비용이 증가 |

FTA (Fault Tree Analysis)를 비교할 때는 단순한 우열보다 어떤 제약 조건에서 어느 방식이 더 적합한지 판단하는 것이 중요하다. 같은 기술도 워크로드, 전력 예산, 검증 난이도, 호환성에 따라 최적 해가 달라진다.

---

## 5. 실무 적용 및 판단

**채택 조건**:
- 시스템 병목이나 구조적 한계를 줄이는 수단으로 FTA (Fault Tree Analysis)을 선택할 근거가 분명할 때
- 상위 소프트웨어와 운영 정책이 해당 구조를 활용할 수 있을 때

**회피 조건**:
- 구현 복잡도와 검증 비용이 기대 효과보다 더 클 때
- 단순한 대안 구조로도 동일한 목표를 달성할 수 있을 때

체크리스트:
1. 적용 목적과 병목 지점이 명확하게 정의되었는가?
2. 성능 향상과 함께 전력·면적·복잡도 증가를 감당할 수 있는가?
3. 상위 소프트웨어와 운영 정책이 해당 구조를 활용할 수 있는가?

---

## 6. 기대효과 및 결론

FTA (Fault Tree Analysis)는 단일 기능으로 끝나는 개념이 아니라, 성능·전력·복잡도·확장성 사이의 균형점을 설계자가 어디에 두는지에 따라 가치가 달라지는 아키텍처 요소다. 앞서 살펴본 구조, 비교, 실무 판단 기준을 종합하면 핵심은 특정 기술을 맹목적으로 채택하는 것이 아니라 시스템 전체 경로에서 병목과 제약을 함께 읽는 데 있다.

또한 실제 효과는 주변 계층과의 정합성이 확보될 때 비로소 나타난다. 하드웨어 단독 최적화만으로는 충분하지 않으며, 소프트웨어 스택·운영 정책·검증 체계·배치 환경까지 함께 맞물려야 안정적인 개선으로 연결된다.

향후에는 더 높은 집적도, 이기종 결합, 자동화된 제어, 보안 강화 요구가 커지면서 FTA (Fault Tree Analysis) 역시 정적 규칙보다 적응형 정책과 도메인 특화 구조 쪽으로 발전할 가능성이 높다. 따라서 실무에서는 개별 장점보다 전체 시스템 균형 속의 채택 타당성을 최종 판단해야 한다.

---

## 7. 발전 흐름도

```text
기존 범용 구조 한계 인식
    ↓
세부 병목·위협·운영 문제 정의
    ↓
FTA (Fault Tree Analysis) 도입
    ↓
하드웨어·소프트웨어 협력 최적화
    ↓
차세대 표준·자동화 체계로 진화
```

---

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| FTA (Fault Tree Analysis) | 해당 주제가 다루는 핵심 메커니즘과 설계 판단의 중심축 |
| 성능 지표 | 처리량·지연 시간·확장성에 미치는 영향을 함께 검토해야 함 |
| 전력·열 | 구현 비용과 지속 가능성을 좌우하는 제약 조건 |
| 신뢰성·보안 | 오류 대응, 검증, 보호 메커니즘과의 연결이 필요 |
| 상위 시스템 | ISA, 운영체제, 컴파일러, 런타임, 인터커넥트와의 정합성이 중요 |
