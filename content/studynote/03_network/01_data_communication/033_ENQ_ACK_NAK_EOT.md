+++
weight = 33
title = "33. ENQ / ACK / NAK / EOT 제어 문자"
date = "2026-03-03"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: **핵심 인사이트 3줄**
> **비유**: ENQ / ACK / NAK / EOT 제어 문자은(는) 데이터통신 기초와 신호 처리 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

BSC(Binary Synchronous Communication)에서 사용하는 제어 문자는 **ASCII 코드 표준(1963)**으로 정의된 비인쇄 문자들이다.

| 문자  | ASCII | 이름                      | 기능        |
|------|-------|--------------------------|------------|
| ENQ  | 0x05  | Enquiry (문의)            | 회선 확보 요청 |
| ACK  | 0x06  | Acknowledgement (긍정 응답) | 수신 성공 확인 |
| NAK  | 0x15  | Negative Acknowledgement | 수신 오류, 재전송 요청 |
| EOT  | 0x04  | End of Transmission (전송 종료) | 회선 해제 |
| STX  | 0x02  | Start of Text           | 본문 시작 |
| ETX  | 0x03  | End of Text             | 본문 종료 |
| SYN  | 0x16  | Synchronous Idle        | 동기 유지 |

---

## 2. 구성요소

### 정상 전송 시나리오

```
송신 측                          수신 측
   │                                │
   │──────── ENQ ──────────────────→│  (회선 확보 요청)
   │←─────── ACK ───────────────────│  (회선 사용 허가)
   │                                │
   │──── STX [데이터] ETX BCC ─────→│  (블록 1 전송)
   │←─────── ACK ───────────────────│  (블록 1 수신 확인)
   │                                │
   │──── STX [데이터] ETX BCC ─────→│  (블록 2 전송)
   │←─────── ACK ───────────────────│  (블록 2 수신 확인)
   │                                │
   │──────── EOT ──────────────────→│  (전송 종료)
```

### 오류 발생 시나리오 (NAK)

```
   │──── STX [데이터] ETX BCC ─────→│  (블록 전송)
   │←─────── NAK ───────────────────│  (CRC 오류 감지)
   │──── STX [데이터] ETX BCC ─────→│  (동일 블록 재전송)
   │←─────── ACK ───────────────────│  (재전송 성공)
```

---

## 3. 구조 및 원리

**핵심 조건**: ENQ / ACK / NAK / EOT 제어 문자은(는) 대역폭, 지연, 신호 대 잡음비 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

BSC에서는 오류 감지를 위해 **ACK0과 ACK1을 교대로 사용**해 블록 번호를 구분한다.

```
블록 1 전송 → ACK1 수신 (홀수 블록 확인)
블록 2 전송 → ACK0 수신 (짝수 블록 확인)
블록 3 전송 → ACK1 수신 (홀수 블록 확인)
...
```

이는 **Stop-and-Wait ARQ의 1비트 순서번호**와 동일한 원리다.

---

## 4. 비교 및 연결

```
BSC ENQ/ACK (1960년대)
        ↓
HDLC I-Frame/S-Frame (1970년대) — 더 효율적인 비트 지향
        ↓
TCP SYN/ACK (1974년~) — 연결 지향, 스트림 기반
  3-Way Handshake: SYN → SYN-ACK → ACK
        ↓
QUIC (2018~) — UDP + ACK, 0-RTT, 손실 기반 혼잡 제어
```

### 현대 프로토콜과의 대응

| BSC 제어 문자 | 현대 프로토콜 대응            |
|-------------|------------------------------|
| ENQ         | TCP SYN / HTTP 요청           |
| ACK         | TCP ACK / HTTP 200 OK         |
| NAK         | TCP NACK / HTTP 400·500      |
| EOT         | TCP FIN / HTTP Connection: close |

---

## 5. 실무 적용 및 판단

산업 자동화에서는 **Modbus RTU 프로토콜**이 ENQ/ACK 개념을 계승한다.

```
마스터(PLC)                    슬레이브(센서/액추에이터)
   │                                   │
   │──[주소][기능코드][데이터][CRC]───→│ (ENQ 역할)
   │←──[주소][기능코드][데이터][CRC]───│ (ACK 역할)
   │←──[주소][오류코드][CRC]───────────│ (NAK 역할: 예외 응답)
```

현대 IoT·SCADA·스마트팩토리에서도 이 원리가 기반이 된다.

---

## 6. 기대효과 및 결론

ENQ / ACK / NAK / EOT 제어 문자을 적용하면 대역폭과 지연에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 데이터통신 기초와 신호 처리를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│             ENQ/ACK/NAK/EOT 발전 흐름                           │
├──────────────┬────────────────────┬─────────────────────────────┤
│ 1960년대     │ BSC·ASCII 정의     │ 제어 문자 체계 표준화        │
│ 1970년대     │ HDLC 비트 지향     │ ACK→RR, NAK→REJ 으로 발전   │
│ 1974년       │ TCP 등장           │ SYN/ACK 3-Way Handshake     │
│ 1980년대     │ Modbus RTU         │ 산업 자동화에 ENQ/ACK 계승   │
│ 2000년대     │ HTTP/SMTP          │ 요청/응답 패러다임 지배적     │
│ 2018년       │ QUIC/HTTP3         │ ACK 최적화, 0-RTT 핸드셰이크 │
└──────────────┴────────────────────┴─────────────────────────────┘

핵심 키워드 연결:
ENQ → 회선 확보 → ACK → 데이터 전송 → EOT
  ↓       ↓        ↓         ↓
오류    폴링    긍정응답   NAK 재전송
  ↓
TCP 3-way → HTTPS → REST API 요청/응답
```

---

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 대역폭 | ENQ / ACK / NAK / EOT 제어 문자를 이해할 때 함께 봐야 하는 데이터통신 기초와 신호 처리 핵심 개념 |
| 지연 | ENQ / ACK / NAK / EOT 제어 문자를 이해할 때 함께 봐야 하는 데이터통신 기초와 신호 처리 핵심 개념 |
| 신호 대 잡음비 | ENQ / ACK / NAK / EOT 제어 문자를 이해할 때 함께 봐야 하는 데이터통신 기초와 신호 처리 핵심 개념 |
| 변조/복조 | ENQ / ACK / NAK / EOT 제어 문자를 이해할 때 함께 봐야 하는 데이터통신 기초와 신호 처리 핵심 개념 |
| 라인 코딩 | ENQ / ACK / NAK / EOT 제어 문자를 이해할 때 함께 봐야 하는 데이터통신 기초와 신호 처리 핵심 개념 |
