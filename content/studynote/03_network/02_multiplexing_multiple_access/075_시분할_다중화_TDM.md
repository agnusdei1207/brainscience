+++
weight = 75
title = "75. 시분할 다중화 (TDM, Time Division Multiplexing) (타임디비전 멀티플렉싱)"
date = "2026-03-30"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: 시분할 다중화 (TDM, Time Division Multiplexing)는 하나의 링크를 시간 슬롯으로 나누어 여러 채널이 순서대로 쓰게 하는 다중화다.
> **비유**: 시분할 다중화 (TDM, Time Division Multiplexing) (타임디비전 멀티플렉싱)은(는) 다중화와 다중접속 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

TDM은 물리 선로를 공유하는 여러 채널이 시간을 번갈아 쓰도록 만든 방식이다. 주파수를 나누는 FDM (Frequency Division Multiplexing)과 달리, 시간 자체를 잘라서 쓰므로 디지털 전송과 궁합이 좋다.

PCM (Pulse Code Modulation)처럼 샘플이 이미 디지털화된 환경에서는, 각 채널의 데이터를 순서대로 실어 나르는 일이 훨씬 쉬워진다. 그래서 TDM은 전용선, 전화망, 산업 제어에서 오래 쓰였다.

```text
FDM: [A====][guard][B====][guard][C====]
TDM: |A|B|C|A|B|C|A|B|C|
```

핵심 차이는 FDM이 공간을 나누는 반면, TDM은 시간을 나눈다는 점이다.

---

## 2. 구성요소

TDM은 MUX와 DeMUX, 그리고 정확한 Clock 동기화로 움직인다. MUX가 각 채널의 데이터를 슬롯에 끼워 넣고, DeMUX는 같은 박자로 그 슬롯을 다시 분해한다.

| 구성 요소 | 역할 | 핵심 포인트 |
| :--- | :--- | :--- |
| MUX | 여러 채널을 하나로 묶음 | 라운드 로빈 |
| Frame | 슬롯 묶음 | 시작 위치 동기화 |
| Time Slot | 한 채널의 배정 시간 | 비어 있으면 낭비 |
| Clock | 송수신 박자 일치 | slip 방지 |
| DeMUX | 다시 분배 | 순서 보존 |

동기화가 조금만 어긋나도 A의 데이터가 B 슬롯으로 밀려 들어간다. 그래서 TDM은 전송률보다 타이밍이 먼저다.

---

## 3. 구조 및 원리

**핵심 조건**: 시분할 다중화 (TDM, Time Division Multiplexing) (타임디비전 멀티플렉싱)은(는) 채널 분할, 슬롯 할당, 충돌 제어 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

TDM의 경계는 FDM과 statistical multiplexing이다. FDM은 주파수를 나누고, TDM은 시간을 나누며, statistical 방식은 실제 트래픽이 있을 때만 자원을 쓴다.

| 비교 축 | TDM | FDM | Statistical Multiplexing |
| :--- | :--- | :--- | :--- |
| 나누는 기준 | 시간 | 주파수 | 큐/패킷 |
| 지연 예측성 | 높음 | 높음 | 변동 |
| 효율 | 슬롯 비면 낮음 | 가드밴드 손실 | 보통 높음 |

그래서 TDM은 버스트성 트래픽보다 일정한 흐름에 더 맞는다. 패킷 스위칭이 널리 쓰이는 이유도 결국 자원 효율 때문이다.

---

## 4. 비교 및 연결

실무에서는 트래픽이 일정하고 지연이 중요한 곳에 TDM을 고려한다. 반대로 웹 트래픽처럼 버스트가 크고 효율이 중요한 환경은 패킷 기반 방식이 더 낫다.

체크 포인트는 다음과 같다.
- 송수신 clock이 실제로 맞는가.
- framing bit이 안정적으로 유지되는가.
- 슬롯 크기와 채널 수가 업무 패턴에 맞는가.

안티패턴은 동기화 장치를 무시하거나, 데이터가 적은데도 큰 프레임을 강제로 유지하는 것이다. 그런 경우 슬롯 낭비가 곧 비용이 된다.

---

## 5. 실무 적용 및 판단

TDM은 단순한 시분할이 아니라, 예측 가능한 전송을 위한 규율이다. 시간 슬롯을 엄격히 나누면 지연은 안정되지만, 유휴 슬롯은 그대로 손실이 된다.

결국 기억해야 할 관점은 "공평한 순번"과 "효율"의 균형이다. TDM은 일정성이 필요한 곳에서 강하고, 불규칙한 데이터에는 덜 적합하다.

---

## 6. 기대효과 및 결론

시분할 다중화 (TDM, Time Division Multiplexing) (타임디비전 멀티플렉싱)을 적용하면 채널 분할과 슬롯 할당에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 다중화와 다중접속 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
channel buffers
    │
    ▼
MUX
    │
    ▼
framed link
    │
    ▼
DeMUX
    │
    ▼
destination channels
```

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
| :--- | :--- |
| Frame | 슬롯의 묶음 |
| Time Slot | 채널별 배정 시간 |
| MUX / DeMUX | 다중화 / 역다중화 |
| Clock | 동기화 기준 |
| FDM | 주파수 분할 비교 대상 |
