+++
weight = 207
title = "207. ARQ 프로토콜 종류"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: **💡 핵심 인사이트**
> **비유**: ARQ 프로토콜 종류은(는) 데이터 링크 계층과 오류 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

데이터가 전선을 타고 날아가면 노이즈 때문에 1비트라도 깨질 확률이 항상 존재합니다.
FEC(전진 오류 수정)처럼 수신기가 스스로 고치려 낑낑대지 않고, **에러를 검출(CRC 등)하자마자 가차 없이 패킷을 쓰레기통에 버린 뒤, 송신기에게 NAK(부정 응답)를 쳐서 재전송을 갈취**해 내는 것이 ARQ의 핵심입니다.

이 핑퐁 게임이 원활하게 돌아가려면 세 가지 무기가 필요합니다.
1. 에러 없이 받았다고 도장을 찍어주는 **ACK** 신호
2. 에러 났다고 징징대는 **NAK** 신호
3. 송신자가 ACK가 올 때까지 무한정 기다리다 굶어 죽는 걸 막기 위한 **타이머(Timeout)**

---

- **철학**: "돌다리도 두들겨 보고 건너자."
- 프레임 1개를 쏘면, 그 1개의 ACK가 올 때까지 송신기는 모든 작동을 멈추고 멍하니 대기합니다. 가장 멍청하지만 구조가 100% 단순해서 에러가 날 확률도 극히 적습니다.

## 2. 구성요소

다음 장(208~210번)에서 하나씩 상세히 다루게 될 세 가지 프로토콜의 진화 과정입니다. 효율성(속도)과 하드웨어 복잡도(메모리 칩셋 가격) 사이의 치열한 타협의 산물입니다.

- **철학**: "일단 여러 개 쏘고, 에러 나면 그 지점부터 싹 다 다시 쏘자."
- 슬라이딩 윈도우(Sliding Window)라는 개념을 도입해, 송신기가 확인도 안 받고 1번부터 5번까지 다다닥 쏘아댑니다. 대역폭 낭비가 사라져 통신 속도가 비약적으로 올라갔지만, 3번이 깨지면 멀쩡히 도착한 4번, 5번까지 모조리 다 다시 쏴야 하는(Go Back) 재전송 낭비가 심합니다.

## 3. 구조 및 원리

**핵심 조건**: ARQ 프로토콜 종류은(는) 프레이밍, 오류 검출, 재전송 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

- **철학**: "에러 난 놈 딱 한 개만 핀셋으로 집어서 다시 쏘자."
- GBN의 재전송 낭비를 막기 위해 탄생한 궁극의 효율성입니다. 3번이 깨지면 4번, 5번은 잘 보관해 두고 오직 3번만 재전송받습니다. 단, 수신기가 도착 순서가 꼬인 패킷들을 예쁘게 재조립해야 하므로 엄청나게 비싸고 똑똑한 메모리 버퍼(Buffer)가 양쪽 모두에게 필요합니다.

## 4. 비교 및 연결

| 관점 | ARQ 프로토콜 종류 | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | 데이터 링크 계층과 오류 제어의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | 프레이밍과 오류 검출을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 재전송·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, ARQ 프로토콜 종류이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- 프레이밍과 오류 검출을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

ARQ 프로토콜 종류을 적용하면 프레이밍과 오류 검출에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 데이터 링크 계층과 오류 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 데이터 링크 계층과 오류 제어 이해
    ↓
ARQ 프로토콜 종류 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

ARQ 프로토콜 종류은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 프레이밍 | ARQ 프로토콜 종류를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 오류 검출 | ARQ 프로토콜 종류를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 재전송 | ARQ 프로토콜 종류를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 흐름 제어 | ARQ 프로토콜 종류를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| MAC/LLC | ARQ 프로토콜 종류를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
