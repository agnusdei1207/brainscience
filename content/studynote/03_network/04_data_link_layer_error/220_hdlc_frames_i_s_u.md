+++
weight = 220
title = "220. 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: **💡 핵심 인사이트**
> **비유**: 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)은(는) 데이터 링크 계층과 오류 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

사용자의 진짜 데이터(사진, 웹페이지 패킷 등)를 가득 싣고 달리는 메인 화물 트럭입니다.

- **제어부 첫 비트**: 무조건 **`0`**으로 시작합니다. 수신기는 첫 비트가 0이면 "오, 진짜 데이터구나!" 하고 정보부(Information Field)를 깝니다.
- **핵심 무기 (피기배킹)**: 이 트럭은 짐만 나르는 게 아닙니다. 제어부 안에 슬라이딩 윈도우를 위한 **`내 전송 순서 번호 N(S)`**와, **`내가 다음번에 너한테 받아야 할 번호 N(R)`**을 같이 적어서 보냅니다. 즉, 내 데이터를 보내면서 상대방이 아까 보낸 데이터의 **수신 확인(ACK)을 업어 태우는(Piggybacking)** 기적의 가성비 프레임입니다.

---

## 2. 구성요소

사용자의 데이터(Information)는 단 1바이트도 싣고 있지 않은, 순수한 통신 통제용 깡통 프레임입니다.

- **제어부 첫 비트**: 무조건 **`10`**으로 시작합니다.
- **역할**: 내가 보낼 데이터(I-프레임)가 없을 때, 상대방에게 **ACK(잘 받았어)나 NAK(에러 났어)만 따로 던져주고 싶거나, 흐름 제어를 할 때** 씁니다.

**[ S-프레임의 4가지 종류 (시험 단골) ]**
1. **RR (Receive Ready)**: `ACK`입니다. "지금까지 잘 받았고, 다음 N번 내놔! 내 버퍼도 널널해!"
2. **RNR (Receive Not Ready)**: `흐름 제어용 ACK`입니다. "지금까지 받은 건 정답이야(ACK). 근데 내 메모리 버퍼 터질 것 같으니까 **제발 다음 거 당분간 쏘지 마(Not Ready)! 기다려!**" (속도 조절 스위치).
3. **REJ (Reject)**: `NAK (Go-Back-N 용)`입니다. "방금 N번 프레임 깨졌어! **N번부터 싹 다 다시 보내!**"
4. **SREJ (Selective Reject)**: `NAK (Selective Repeat 용)`입니다. "딴 건 됐고, **딱 N번 프레임 하나만 다시 보내봐.**"

---

## 3. 구조 및 원리

**핵심 조건**: 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)은(는) 프레이밍, 오류 검출, 재전송 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

통신을 시작하기 전에 "선 좀 꼽자", "이제 끊자" 등 연결 셋업을 담당하는 관리자입니다.
순서 번호(Sequence Number)가 전혀 안 적혀 있어서 Unnumbered(비번호)라고 부릅니다.

- **제어부 첫 비트**: 무조건 **`11`**로 시작합니다.
- **역할**: 에러 검출 이런 게 아니라, 링크를 처음 열 때(SABM: 비동기 균형 모드로 연결하자!), 에러 나서 링크를 리셋할 때, 접속을 끊고 집에 갈 때(DISC: Disconnect) 던지는 최상위 시스템 제어 명령어들입니다.

## 4. 비교 및 연결

| 관점 | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame) | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | 데이터 링크 계층과 오류 제어의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | 프레이밍과 오류 검출을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 재전송·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- 프레이밍과 오류 검출을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)을 적용하면 프레이밍과 오류 검출에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 데이터 링크 계층과 오류 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 데이터 링크 계층과 오류 제어 이해
    ↓
정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame) 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 프레이밍 | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 오류 검출 | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 재전송 | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 흐름 제어 | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| MAC/LLC | 정보 프레임(I-Frame), 감독/제어(S-Frame / RR, RNR, REJ, SREJ), 비번호(U-Frame)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
