+++
weight = 226
title = "226. NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: NCP (Network Control Protocol)는 PPP (Point-to-Point Protocol) 연결의 마지막 단계에서, IP나 IPX 등 상위 네트워크 계층 프로토콜을 협상하고 동적으로 설정하기 위한 제어 프로토콜의 집합이다.
> **비유**: NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)은(는) 데이터 링크 계층과 오류 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

- **개념**: NCP (Network Control Protocol)는 데이터 링크 계층(Layer 2) 통신 규약인 PPP 내에서, 상위 3계층 네트워크 프로토콜을 초기화하고 협상하기 위해 사용되는 하위 프로토콜들의 총칭이다. IP용으로는 IPCP (IP Control Protocol), IPv6용으로는 IPv6CP가 사용된다.

- **필요성**: 두 노드 간 물리적 연결이 완료되고(LCP 단계) 사용자가 인증되었다고 해서 곧바로 인터넷이 되는 것은 아니다. 서로 통신하려면 양단이 IP 주소를 가져야 하고, 헤더 압축 여부 등을 맞춰야 한다. NCP는 이러한 상위 네트워크 논리적 구성을 전담하여 링크를 실사용 가능한 상태로 만든다.

---

PPP 연결은 크게 세 단계를 거친다.
1. **LCP (Link Control Protocol)**: 링크 설정, MTU 협상, 루프백 탐지.
2. **인증 단계 (선택)**: PAP, CHAP 등을 통한 사용자 확인.
3. **NCP (Network Control Protocol)**: 네트워크 계층 프로토콜 설정 (IP 주소 등 할당). 이 단계가 성공해야 데이터그램이 교환된다.

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                     PPP 연결 상태 천이도                    │
 ├─────────────────────────────────────────────────────────────┤
 │                                                             │
 │   [Dead] ────(물리적 연결)───▶ [Establish]                  │
 │                                    │  (LCP 협상 진행)       │
 │                                    ▼                        │
 │  ┌─────────────── [Authenticate] ◀─┘                        │
 │  │ (인증 실패)        │                                     │
 │  ▼                    ▼ (인증 성공 또는 인증 없음)          │
 │ [Terminate] ◀────── [Network]  (NCP 협상 진행)              │
 │                       │                                     │
 │                       ▼ (IPCP 등 NCP 설정 완료)             │
 │                     [Open]  ◀─── 실제 IP 데이터 전송 상태!  │
 │                                                             │
 └─────────────────────────────────────────────────────────────┘
```

## 2. 구성요소

가장 대표적인 NCP인 IPCP(RFC 1332)의 주요 기능은 다음과 같다.
- **IP 주소 할당**: 다이얼업이나 PPPoE 연결 시 ISP 서버가 클라이언트에게 동적 IP를 부여한다.
- **헤더 압축 협상**: Van Jacobson TCP/IP 헤더 압축 등을 사용할지 협상하여 대역폭 효율을 높인다.

## 3. 구조 및 원리

**핵심 조건**: NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)은(는) 프레이밍, 오류 검출, 재전송 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

NCP 덕분에 PPP는 하나의 시리얼 라인 위에서 IP, IPv6, IPX 데이터그램을 섞어서 전송할 수 있다. 프레임의 `Protocol` 필드를 통해 수신 측이 이를 식별한다. (예: 0x8021은 IPCP, 0x0021은 IP 데이터)

## 4. 비교 및 연결

| 관점 | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP) | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | 데이터 링크 계층과 오류 제어의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | 프레이밍과 오류 검출을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 재전송·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- 프레이밍과 오류 검출을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)을 적용하면 프레이밍과 오류 검출에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 데이터 링크 계층과 오류 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 데이터 링크 계층과 오류 제어 이해
    ↓
NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP) 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 프레이밍 | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 오류 검출 | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 재전송 | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| 흐름 제어 | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
| MAC/LLC | NCP (Network Control Protocol) - 네트워크 계층 동적 설정 (IPCP, IPXCP)를 이해할 때 함께 봐야 하는 데이터 링크 계층과 오류 제어 핵심 개념 |
