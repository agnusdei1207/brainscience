+++
weight = 246
title = "246. IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: IEEE 802.1Q는 서로 다른 스위치 간에 여러 개의 가상 랜(VLAN) 데이터를 하나의 물리적 케이블(트렁크 포트)로 섞어 보낼 때, 각 프레임이 어느 VLAN 소속인지 구별하기 위해 **이더넷 헤더 중간에 4바이트짜리 꼬리표(Tag)를 끼워 넣는 국제 표준 규격**이다.
> **비유**: IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)은(는) LAN/WAN 구조와 2계층 장비 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

- **개념**: 802.1Q는 이더넷 프레임에 소속 VLAN 정보를 명시하는 '태깅(Tagging)' 기술의 IEEE 공식 표준이다.
- **필요성**: 만약 A 스위치에 영업부(VLAN 10)와 인사부(VLAN 20)가 있고, B 스위치에도 영업부와 인사부가 있다고 치자. 두 스위치를 1가닥의 선(트렁크)으로 연결하면, B 스위치는 건너온 데이터가 영업부 것인지 인사부 것인지 알 도리가 없다. 따라서 건너가는 택배 상자 겉면에 "이건 10번 방(VLAN 10) 물건이야!"라는 형광색 스티커를 붙여주어야 하는데, 이것이 바로 802.1Q 태그다.

---

이더넷 프레임의 앞부분은 `[목적지 MAC] - [출발지 MAC] - [Type] - [Data]` 순으로 되어 있다. 802.1Q 스위치는 프레임을 밖으로 쏠 때, **[출발지 MAC]과 [Type] 사이를 강제로 벌리고 4바이트(32비트)의 태그를 삽입**한다.
- 태그가 삽입되면서 원래 있던 FCS(에러 검출 코드) 값은 틀려지므로, 스위치가 FCS 값을 새롭게 다시 계산하여 맨 꼬리에 덮어쓴다.

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                IEEE 802.1Q 프레임 태깅 구조 도식               │
 ├─────────────────────────────────────────────────────────────┤
 │                                                             │
 │   [ 일반 이더넷 프레임 (Untagged) ]                            │
 │   ┌────────┬────────┬───────┬──────────────────────┬─────┐  │
 │   │목적지MAC│출발지MAC │ Type  │   Payload (IP 패킷)   │ FCS │  │
 │   └────────┴────────┴───────┴──────────────────────┴─────┘  │
 │                       ▲ 여기를 벌리고 끼워 넣음               │
 │                                                             │
 │   [ 802.1Q 태그 이더넷 프레임 (Tagged) ]                       │
 │   ┌────────┬────────┬───────┬───────┬──────────────┬─────┐  │
 │   │목적지MAC│출발지MAC │ 802.1Q│ Type  │ Payload (IP) │ New │  │
 │   │        │        │ TAG(4B│       │              │ FCS │  │
 │   └────────┴────────┴───────┴───────┴──────────────┴─────┘  │
 │                      │       │                              │
 │                      └── ┬ ──┘                              │
 │                           ▼ 4바이트(32비트)의 내부 구조         │
 │                      [ TPID(16) | TCI(16) ]                 │
 │                                                             │
 └─────────────────────────────────────────────────────────────┘
```

## 2. 구성요소

태그는 크게 2바이트의 **TPID**와 2바이트의 **TCI**로 나뉜다.

1. **TPID (Tag Protocol Identifier, 16비트)**:
   - 항상 `0x8100`이라는 고정된 값을 가진다.
   - 수신 스위치가 이 값을 보면 "아, 이 프레임은 일반 프레임이 아니라 802.1Q 태그가 붙은 프레임이구나!"라고 단번에 인식(식별)하게 해주는 마법의 번호다.
2. **TCI (Tag Control Information, 16비트)**:
   - **PRI (Priority, 3비트)**: `IEEE 802.1p`라고 불리는 음성/영상 트래픽 우선순위(QoS)를 부여하는 비트. 0~7까지의 값을 갖는다.
   - **CFI (Canonical Format Indicator, 1비트)**: 토큰링 등 과거 망과의 호환성을 위한 비트 (현재는 거의 `0`으로 고정).
   - **VID (VLAN Identifier, 12비트) ★핵심**: 자신이 속한 VLAN 번호를 나타낸다. 12비트이므로 $2^{12} = 4096$개의 숫자를 표현할 수 있다. (0과 4095는 예약되어 실제 사용 가능 범위는 1~4094번이다.)

## 3. 구조 및 원리

**핵심 조건**: IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)은(는) Ethernet, MAC 주소, 스위칭 제약 안에서 일관된 제어 규칙을 유지할 때 효과가 난다.

동작 순서:
1. 대상 자원과 상태 정보를 수집한다.
2. 정책 또는 프로토콜 규칙에 따라 처리 기준을 결정한다.
3. 데이터 전달·주소 지정·세션 유지·오류 처리 중 필요한 제어를 수행한다.
4. 결과를 상위 계층 또는 인접 장비와 연동한다.
5. 성능 지표를 확인하여 병목과 예외 상황을 보정한다.

```text
[요구사항/트래픽 특성]
          ↓
[IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)]
          ↓
[전달·제어·보호 처리]
          ↓
[성능 검증 및 최적화]
```

## 4. 비교 및 연결

| 관점 | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가) | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | LAN/WAN 구조와 2계층 장비의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | Ethernet과 MAC 주소을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 스위칭·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- Ethernet과 MAC 주소을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)을 적용하면 Ethernet과 MAC 주소에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 LAN/WAN 구조와 2계층 장비를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 LAN/WAN 구조와 2계층 장비 이해
    ↓
IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가) 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| Ethernet | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)를 이해할 때 함께 봐야 하는 LAN/WAN 구조와 2계층 장비 핵심 개념 |
| MAC 주소 | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)를 이해할 때 함께 봐야 하는 LAN/WAN 구조와 2계층 장비 핵심 개념 |
| 스위칭 | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)를 이해할 때 함께 봐야 하는 LAN/WAN 구조와 2계층 장비 핵심 개념 |
| VLAN | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)를 이해할 때 함께 봐야 하는 LAN/WAN 구조와 2계층 장비 핵심 개념 |
| 브리지/STP | IEEE 802.1Q - VLAN 태깅(Tagging), TPID, TCI, VID 필드 (4바이트 추가)를 이해할 때 함께 봐야 하는 LAN/WAN 구조와 2계층 장비 핵심 개념 |
