+++
weight = 357
title = "357. OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: OSPF(Open Shortest Path First)는 전 세계 모든 대기업과 통신사의 사내망(IGP)을 사실상 100% 장악하고 있는 **업계 표준(Open)의 제왕이자, 다익스트라(SPF) 알고리즘을 돌려 가장 빠르고 넓은 길(Cost)을 찾아내는 링크 상태(Link-State) 라우팅 프로토콜의 대명사**다.
> **비유**: OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜은(는) 라우팅과 경로 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

- **개념**: IP 데이터그램을 통해 라우터 간에 링크 상태 정보를 교환하고, SPF 알고리즘을 사용해 루프 없는 최단 경로 트리를 구성하는 동적 라우팅 프로토콜 (RFC 2328). IP 프로토콜 번호 89번을 사용한다.
- **필요성**: 1980년대 후반, 인터넷이 폭발적으로 커지면서 RIP(최대 15대 라우터 제한)로는 거대해진 회사망을 도저히 커버할 수 없었다. 게다가 속도가 10Mbps든 1Gbps든 똑같이 1점(Hop)으로 치는 RIP의 멍청함 때문에 트래픽 병목이 심각했다. "라우터 대수 제한(Hop)을 없애고, 선로의 **'대역폭(속도)'**을 기준으로 가장 빵빵 뚫린 고속도로를 찾아내는, 특정 회사(Cisco)에 종속되지 않은 **'개방형(Open)'** 표준 프로토콜이 절실하다!"

---

- **Open**: 시스코 장비, 주니퍼 장비, HP 장비 등 제조사가 달라도 완벽하게 호환되어 대화를 나눌 수 있다. (반면 EIGRP는 시스코 장비끼리만 통한다).
- **멀티캐스트**: RIPv1처럼 브로드캐스트(`255.255.255.255`)로 시끄럽게 떠들지 않는다. OSPF 라우터들만 듣는 조용한 단톡방인 **`224.0.0.5` (모든 OSPF 라우터)**와 **`224.0.0.6` (DR/BDR 전용)** 멀티캐스트 주소를 사용하여 PC들의 CPU를 전혀 방해하지 않는다.

## 2. 구성요소

OSPF가 길의 1등과 2등을 가르는 점수(메트릭)는 **Cost(비용)**다.
무조건 숫자가 낮을수록(싸야) 1등 길이다.

**계산 공식**: $\text{Cost} = \frac{10^8 (\text{기준 대역폭, 100Mbps})}{\text{해당 인터페이스의 대역폭(bps)}}$

- **10Mbps 이더넷**: $100,000,000 / 10,000,000$ = **Cost 10**
- **100Mbps 패스트이더넷**: $100,000,000 / 100,000,000$ = **Cost 1**
- **1Gbps 기가비트**: 어? 공식대로면 $0.1$인데 소수점은 안 되므로 무조건 **Cost 1**이다.
- *실무 팁*: 요즘은 1Gbps, 10Gbps 망이 흔한데 저 공식을 그대로 쓰면 전부 다 Cost 1로 동점이 되어 버린다(기가비트와 10기가비트를 구별 못 함). 그래서 실무에서는 기준 대역폭 $10^8$을 $10^{10}$ 등으로 강제로 높이는 명령어(`auto-cost reference-bandwidth`)를 반드시 박아 넣어야 최신 고속망을 제대로 라우팅한다.

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                RIP (Hop) vs OSPF (Cost) 라우팅 판단 차이         │
 ├─────────────────────────────────────────────────────────────┤
 │                                                             │
 │   [ 내 라우터 ] ──── (1.5Mbps T1 전용선) ───▶ [ 목적지 ]     │
 │        │                                              ▲     │
 │        └── (1Gbps 광랜) ──▶ [중간 라우터] ── (1Gbps) ──┘     │
 │                                                             │
 │   * RIP의 멍청함: "윗길은 라우터 0대(1점), 아랫길은 라우터 1대(2점)네!" │
 │                 ──▶ 느려터진 윗길로 데이터 쏘다가 망함.          │
 │                                                             │
 │   * OSPF의 똑똑함: "윗길 Cost=64, 아랫길 Cost=1+1=2 네!"         │
 │                 ──▶ 당연히 Cost가 싼 1Gbps 아랫길로 우회함.      │
 └─────────────────────────────────────────────────────────────┘
```

## 3. 구조 및 원리

**핵심 조건**: OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜은(는) 라우팅 프로토콜, 경로 계산, 정책 제어 조건을 함께 만족할 때 안정적으로 동작한다.

동작 순서:

OSPF는 벨만-포드(거리 벡터)처럼 맹목적인 소문 덧셈을 하지 않고, 다익스트라(Dijkstra) 수학 공식을 써서 내 눈앞에 펼쳐진 전체 지형도(LSDB)에 직접 선을 긋기 때문에, 태생적으로 **절대로 라우팅 루프(빙빙 도는 현상)에 빠지지 않는 구조적 완벽함**을 자랑한다.

## 4. 비교 및 연결

| 관점 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜 | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | 라우팅과 경로 제어의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | 라우팅 프로토콜과 경로 계산을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 정책 제어·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- 라우팅 프로토콜과 경로 계산을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜을 적용하면 라우팅 프로토콜과 경로 계산에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 라우팅과 경로 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 라우팅과 경로 제어 이해
    ↓
OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 라우팅 프로토콜 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜를 이해할 때 함께 봐야 하는 라우팅과 경로 제어 핵심 개념 |
| 경로 계산 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜를 이해할 때 함께 봐야 하는 라우팅과 경로 제어 핵심 개념 |
| 정책 제어 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜를 이해할 때 함께 봐야 하는 라우팅과 경로 제어 핵심 개념 |
| 수렴 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜를 이해할 때 함께 봐야 하는 라우팅과 경로 제어 핵심 개념 |
| 보안 | OSPF (Open Shortest Path First) - 대표적 링크 상태 프로토콜를 이해할 때 함께 봐야 하는 라우팅과 경로 제어 핵심 개념 |
