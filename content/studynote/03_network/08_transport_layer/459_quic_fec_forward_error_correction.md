+++
weight = 459
title = "459. FEC 기능 선택적 포함 (초기)"
date = "2026-05-05"
[extra]
categories = "studynote-network"
+++

## 0. 핵심 인사이트

> **핵심**: FEC(전진 에러 수정)는 통신 중에 패킷 하나가 날아가면 상대방이 "그거 다시 보내!"라고 재전송(Retransmission)을 요구하는 촌스러운 짓을 막기 위해, **애초에 데이터를 보낼 때 "패킷 4개 보낼 때 복구용 정답지(패리티 패킷) 1개 더 얹어서" 쏴버림으로써, 수신자가 유실된 패킷을 스스로 유추하고 복원해 내게 만드는 씹상남자식 마법 기술**이다.
> **비유**: FEC 기능 선택적 포함 (초기)은(는) 전송 계층의 신뢰성과 세션 제어 안에서 데이터와 제어 규칙을 질서 있게 맞춰 주는 교통 체계와 같다.

> 📝 모범 답안

## 1. 개요 및 필요성

- **개념**: 데이터 블록(Data)들에 수학적인 오류 정정 코드(Redundant/Parity bit)를 추가로 붙여 전송함으로써, 네트워크 상에서 일부 패킷이 유실되더라도 수신 측이 재전송 요청(ARQ) 없이 독립적으로 유실된 데이터를 복원해 내는 채널 코딩 기술. (초기 구글 QUIC에서 실험적으로 도입됨).
- **필요성**: 화성 탐사선이나 해저 케이블 통신을 해보자. 미국에서 한국까지 한 번 쏘는데 200ms가 걸린다. 100번 패킷이 바다에 빠졌다. 한국 PC가 "100번 다시 줘!(ACK)" 쏘고, 미국 서버가 "옛다 100번!(재전송)" 쏴주는 걸 기다리려면 400ms(0.4초)가 허공에 날아간다. **"야! 왕복 시간(RTT) 기다리는 거 개빡치니까, 애초에 내가 보낼 때 '만능 복구 패킷'을 하나 끼워 팔 테니까, 가다가 하나 빠져도 네가 그 만능 패킷으로 수학 계산해서 빈칸 채워 넣어!! 재전송 요구 절대 하지 마!!"**

---

- 구글 서버가 4개의 패킷 `A, B, C, D`를 보낸다 치자.
- 송신자(서버)는 이 4개의 덩어리를 `XOR` 연산 믹서기에 넣고 윙 돌린다.
- 결과물로 **`패리티 패킷 P`**가 1개 뿅! 하고 튀어나온다.
- 서버는 `A, B, C, D, P` 총 5개를 쏜다.
- **사고 발생!**: 패킷 `C` 가 바다에 빠져서 영원히 죽었다.
- 수신자(스마트폰)는 `A, B, _, D, P` 4개만 받았다.
- **복구의 기적**: 수신자는 놀라지 않고, 멀쩡히 받은 `A, B, D` 그리고 힌트 엽서인 `P`를 믹서기(역 XOR 연산)에 다시 넣고 갈아버린다.
- 믹서기 안에서 마법처럼 죽어버린 **`C`의 원본 데이터가 100% 완벽하게 튀어나온다!!**

## 2. 구성요소

네트워크 기사 실기시험이나 면접에서 FEC의 원리를 물어보면 무조건 'XOR(배타적 논리합)' 연산을 답해야 한다.

이론만 들으면 "와! 재전송(RTT 지연) 없이 100% 복구되네? 이거 왜 TCP에 안 씀?" 싶다.
구글도 그렇게 생각하고 크롬(Chrome) 초기 버전에 이 기능을 무식하게 때려 박았다. 하지만 실전은 달랐다.

1. **대역폭의 미친 낭비**: 안 그래도 패킷이 꽉 차서 혼잡한 인터넷인데, 4개 보낼 때마다 쓸데없는 1개(P)를 계속 덧붙여 보내니 인터넷 전체가 쓰레기 데이터로 1.2배 터져나갔다. (특히 패킷 유실률이 낮은 유선망에서는 완전 돈 낭비).
2. **스마트폰 배터리 살살 녹는다**: 복구용 힌트 패킷(P)을 풀기 위해 스마트폰 CPU가 미친 듯이 XOR 수학 공식을 돌려야 한다. 영상을 1시간 봤을 뿐인데 폰이 핫팩처럼 뜨거워지고 배터리가 증발했다.

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                초기 QUIC의 FEC와 현대 IETF QUIC의 차이           │
 ├─────────────────────────────────────────────────────────────┤
 │                                                             │
 │   [ 2013년 구글의 초기 QUIC (gQUIC) ]                         │
 │   "야! 재전송은 구시대의 유물이야! 무조건 XOR 갈겨서 FEC 덧붙여 쏴!"│
 │   ──▶ 배터리 광탈 + 오버헤드 낭비로 개같이 욕먹음.                  │
 │                                                             │
 │   [ 2021년 표준 IETF QUIC (RFC 9000) ]                        │
 │   "구글아, 깝치지 말고 FEC 빼라. 그냥 기존 TCP 놈들이 쓰던           │
 │    선택적 확인 응답(SACK) 업그레이드해서 재전송(Retransmit) 하는 게 │
 │    가성비 젤 좋다."                                            │
 │                                                             │
 │   ▶ "결국 현재 우리가 쓰는 HTTP/3(QUIC)에는 FEC 기능이 빠져있거나,   │
 │      아예 안 쓰는(Deprecated) 모듈로 구석에 처박혀 있다."           │
 └─────────────────────────────────────────────────────────────┘
```

## 3. 구조 및 원리

**핵심 조건**: FEC 기능 선택적 포함 (초기)은(는) TCP/UDP, 순서 제어, 재전송 제약 안에서 일관된 제어 규칙을 유지할 때 효과가 난다.

동작 순서:
1. 대상 자원과 상태 정보를 수집한다.
2. 정책 또는 프로토콜 규칙에 따라 처리 기준을 결정한다.
3. 데이터 전달·주소 지정·세션 유지·오류 처리 중 필요한 제어를 수행한다.
4. 결과를 상위 계층 또는 인접 장비와 연동한다.
5. 성능 지표를 확인하여 병목과 예외 상황을 보정한다.

```text
[요구사항/트래픽 특성]
          ↓
[FEC 기능 선택적 포함 (초기)]
          ↓
[전달·제어·보호 처리]
          ↓
[성능 검증 및 최적화]
```

## 4. 비교 및 연결

| 관점 | FEC 기능 선택적 포함 (초기) | 인접 기술/대안 | 판단 포인트 |
|:---|:---|:---|:---|
| 적용 범위 | 전송 계층의 신뢰성과 세션 제어의 특정 문제를 직접 해결 | 상위·하위 계층 또는 대체 방식이 일부 기능을 분담 | 요구사항과 계층 경계 확인 |
| 장점 | TCP/UDP과 순서 제어을 구조적으로 통제 | 범용성 또는 단순성이 강점 | 목적에 맞는 선택 필요 |
| 한계 | 재전송·운영 복잡도 증가 가능 | 기능은 단순하지만 제어력 제한 | 비용·표준 호환성 검토 |

핵심은 단순 우열 비교가 아니라, FEC 기능 선택적 포함 (초기)이 어떤 맥락에서 더 적합한지와 인접 기술과의 역할 분담을 해석하는 데 있다.

## 5. 실무 적용 및 판단

**채택 조건**
- TCP/UDP과 순서 제어을 동시에 관리해야 할 때
- 운영 자동화 또는 표준화된 제어가 필요할 때
- 장애 분석 시 원인 구분이 명확해야 할 때

**회피 조건**
- 단순 환경이라 오버헤드가 더 큰 경우
- 장비·프로토콜 호환성이 확보되지 않은 경우
- 측정 지표 없이 관행적으로만 적용하는 경우

**체크리스트**
1. 요구 성능과 허용 지연이 정의되었는가?
2. 오류·혼잡·보안 처리 경계가 명확한가?
3. 표준·벤더 구현 차이를 검증했는가?
4. 운영 관측성 및 장애 대응 절차가 준비되었는가?

## 6. 기대효과 및 결론

FEC 기능 선택적 포함 (초기)을 적용하면 TCP/UDP과 순서 제어에 대한 통제력이 높아지고, 운영 일관성과 성능 예측 가능성을 확보할 수 있다. 반면 설정 복잡도, 표준 호환성, 장비 비용, 운영 숙련도라는 전제가 뒤따르므로 무조건적인 도입이 정답은 아니다. 결론적으로 이 주제는 전송 계층의 신뢰성과 세션 제어를 이해하는 기준점이며, 최근에는 자동화·가상화·지능형 제어와 결합해 더 세밀한 최적화 방향으로 진화하고 있다.

## 7. 발전 흐름도

```text
기초 전송 계층의 신뢰성과 세션 제어 이해
    ↓
FEC 기능 선택적 포함 (초기) 개념 정립
    ↓
표준화·상용화 적용
    ↓
자동화·가상화 연계
    ↓
지능형 최적화·차세대 확장
```

FEC 기능 선택적 포함 (초기)은(는) 단일 기능으로 끝나지 않고 표준화, 운영 자동화, 고속화·지능화 단계로 확장된다.

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| TCP/UDP | FEC 기능 선택적 포함 (초기)를 이해할 때 함께 봐야 하는 전송 계층의 신뢰성과 세션 제어 핵심 개념 |
| 순서 제어 | FEC 기능 선택적 포함 (초기)를 이해할 때 함께 봐야 하는 전송 계층의 신뢰성과 세션 제어 핵심 개념 |
| 재전송 | FEC 기능 선택적 포함 (초기)를 이해할 때 함께 봐야 하는 전송 계층의 신뢰성과 세션 제어 핵심 개념 |
| 흐름/혼잡 제어 | FEC 기능 선택적 포함 (초기)를 이해할 때 함께 봐야 하는 전송 계층의 신뢰성과 세션 제어 핵심 개념 |
| 포트 | FEC 기능 선택적 포함 (초기)를 이해할 때 함께 봐야 하는 전송 계층의 신뢰성과 세션 제어 핵심 개념 |
