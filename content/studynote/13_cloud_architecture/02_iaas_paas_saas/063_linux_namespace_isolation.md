+++
weight = 63
title = "63. 리눅스 네임스페이스 (Namespace) - PID, Net, Mount, User 등 자원 분리"
date = "2026-04-07"
[extra]
categories = "studynote-cloud-architecture"
+++

## 0. 핵심 인사이트

> **핵심**: Linux Namespace는 프로세스가 보는 PID, 네트워크, 마운트, 사용자 등의 시야를 분리하는 커널 격리 기능이다.
> **비유**: 복잡한 시스템을 작은 운영 규칙으로 정리해 안정적으로 굴러가게 만드는 교통 관제 체계와 같다.

---

> 📝 모범 답안

## 1. 개요 및 필요성

하나의 리눅스 커널을 여러 서비스가 함께 쓰면, 서로의 프로세스나 파일 시스템이 보이는 문제가 생긴다. Namespace는 "보이는 세계"를 나눠서 이런 충돌을 줄인다.

컨테이너가 가볍게 느껴지는 이유도 결국 이 때문이다. 각 프로세스가 자기만의 작은 우주를 가진 것처럼 보이게 만들기 때문이다.

---

## 2. 구성요소

```text
Process
  ├─ PID Namespace
  ├─ NET Namespace
  ├─ MNT Namespace
  ├─ UTS Namespace
  ├─ IPC Namespace
  ├─ USER Namespace
  └─ CGROUP Namespace
```

| Namespace | 분리 대상 | 효과 |
| :-- | :-- | :-- |
| PID | 프로세스 ID | 각 컨테이너가 자기 PID 1을 가짐 |
| NET | 네트워크 스택 | 인터페이스, 포트, 라우팅 분리 |
| MNT | 마운트 공간 | 서로 다른 파일 시스템 보기 |
| UTS | 호스트명 | 컨테이너별 hostname 분리 |
| IPC | 프로세스 간 통신 | 공유 메모리/세마포어 분리 |
| USER | 사용자/권한 | root 매핑과 권한 격리 |

Namespace는 "누가 무엇을 볼 수 있느냐"를 바꾸고, cgroups는 "얼마나 쓸 수 있느냐"를 바꾼다. 둘은 역할이 다르다.

---

## 3. 구조 및 원리

**핵심 조건**: 컨테이너/쿠버네티스 환경에서는 선언형 상태, 스케줄링 정책, 네트워크·스토리지 연동, 복구 자동화가 일관되게 맞물려야 운영 안정성이 확보된다.

동작 순서:
1. 사용자가 원하는 최종 상태를 선언형으로 정의한다.
2. 컨트롤 플레인이 스케줄링과 리소스 배치를 결정한다.
3. 런타임이 컨테이너를 기동하고 네트워크·스토리지를 연계한다.
4. 서비스 계층이 외부 트래픽과 내부 통신을 안정적으로 중계한다.
5. 프로브·오토스케일링·정책 엔진이 복구와 최적화를 반복한다.

| 구분 | Namespace | cgroups | chroot |
| :-- | :-- | :-- | :-- |
| 목적 | 시야 분리 | 자원 제한 | 파일 경로 제한 |
| 강점 | 프로세스/네트워크/마운트 격리 | CPU, 메모리, I/O 통제 | 단순하고 가벼움 |
| 한계 | 자원 사용량 제한은 못함 | 보이는 세계 분리는 못함 | 보안 격리가 약함 |

Namespace는 컨테이너의 기반이지만, 단독으로는 충분하지 않다. 보안과 운영 안정성을 위해서는 권한, cgroups, 보안 모듈까지 함께 설계해야 한다.

---

## 4. 비교 및 연결

### 체크리스트

1. 필요한 Namespace 종류를 모두 적용했는가?
2. USER Namespace로 권한 매핑을 분리했는가?
3. NET Namespace와 방화벽 정책이 함께 설계되었는가?
4. cgroups와 같이 자원 한도가 설정되었는가?
5. unshare/setns/clone 기반 흐름을 이해하고 있는가?

### 안티패턴

- Namespace만 믿고 리소스 고갈을 방치하는 설계
- root 권한 공유를 그대로 두는 설계
- 네트워크 Namespace와 보안 그룹을 혼동하는 설계
- 파일 시스템 분리만 하고 프로세스 격리는 놓치는 설계

기술사 관점에서는 Namespace를 "컨테이너 기능"으로만 보지 말고, 리눅스 커널의 격리 메커니즘으로 이해해야 한다. 그래야 보안과 운영을 동시에 설명할 수 있다.

---

## 5. 실무 적용 및 판단

Namespace는 리눅스 한 개를 여러 개의 작은 리눅스처럼 보이게 만든다. 이 덕분에 컨테이너, 샌드박스, 프로세스 분리가 모두 가능해졌다.

결국 핵심은 "보이는 세상"을 분리하는 능력이며, 이것이 현대 컨테이너의 첫 번째 벽이다.

---

## 6. 기대효과 및 결론

리눅스 네임스페이스 (Namespace) - PID, Net, Mount, User 등 자원 분리를 올바르게 적용하면 확장성, 운영 안정성, 자동화 수준, 가시성을 함께 높일 수 있다. 다만 이 효과는 기술 자체만으로 생기지 않으며, 책임 경계·표준화·보안 통제·비용 관리가 함께 설계될 때 비로소 현실화된다.

향후에는 관리형 서비스, 정책 자동화, AI 기반 운영 최적화와 결합되면서 리눅스 네임스페이스 (Namespace) - PID, Net, Mount, User 등 자원 분리의 중요성이 더 커질 것이다. 따라서 핵심은 특정 도구를 도입하는 것이 아니라, 업무 특성과 조직 역량에 맞는 적용 범위와 운영 모델을 설계하는 데 있다.

---

## 7. 발전 흐름도

```text
chroot
  ↓
Namespace
  ↓
cgroups
  ↓
Container Runtime
```

---

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| 컨테이너 런타임 | 이미지 실행과 격리의 하위 계층 |
| 쿠버네티스 | 선언형 오케스트레이션의 표준 제어 평면 |
| CNI/CSI | 네트워크와 스토리지 확장을 담당하는 표준 인터페이스 |
| Ingress/Service | 트래픽 진입과 서비스 노출을 담당 |
| 오토스케일링 | 부하에 맞춰 파드와 노드를 자동 조정 |

---
