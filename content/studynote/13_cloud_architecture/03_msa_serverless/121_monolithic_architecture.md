+++
weight = 121
title = "121. 모놀리식 아키텍처 (Monolithic Architecture) - 단일체 구조의 특징과 한계"
date = "2026-04-19"
[extra]
categories = "studynote-cloud-architecture"
+++

## 0. 핵심 인사이트

> **핵심**: 모놀리식 아키텍처는 **전체 애플리케이션이 하나의 코드베이스·빌드·배포 단위로 구성**되는 전통적 구조이며, 모든 기능(UI·비즈니스 로직·데이터 접근)이 하나의 프로세스에서 실행된다.
> **비유**: 복잡한 시스템을 작은 운영 규칙으로 정리해 안정적으로 굴러가게 만드는 교통 관제 체계와 같다.

---

> 📝 모범 답안

## 1. 개요 및 필요성

```text
┌───────────────────────────────────────────────────────┐
│    모놀리식 vs MSA                                    │
├───────────────────────────────────────────────────────┤
│  [모놀리식]                [MSA]                      │
│  ┌──────────────┐        ┌───┐ ┌───┐ ┌───┐          │
│  │ UI           │        │Svc│ │Svc│ │Svc│          │
│  │ BizLogic     │        │ A │ │ B │ │ C │          │
│  │ DataAccess   │        └───┘ └───┘ └───┘          │
│  │ 단일 DB      │          ↕     ↕     ↕            │
│  └──────────────┘        DB_A  DB_B  DB_C            │
│  하나의 배포 단위         독립 배포·스케일링           │
└───────────────────────────────────────────────────────┘
```

---

## 2. 구성요소

### 모놀리식의 장단점

| 장점 | 단점 |
|:---|:---|
| 개발·디버깅 단순 | 빌드 시간 ↑ (규모 증가 시) |
| 트랜잭션 관리 쉬움 | **부분 배포 불가** |
| 호출 오버헤드 없음 | **장애 전파 (단일 프로세스)** |
| 초기 속도 빠름 | 팀 간 코드 충돌 |

---

## 3. 구조 및 원리

**핵심 조건**: 분산 서비스 환경에서는 서비스 경계, 상태 관리, 호출 정책, 장애 격리, 데이터 일관성 전략이 명확해야 전체 시스템이 안정적으로 동작한다.

동작 순서:
1. 요청이 API 게이트웨이·이벤트 소스·메시지 브로커를 통해 유입된다.
2. 서비스 디스커버리와 라우팅 계층이 적절한 실행 주체를 결정한다.
3. 각 서비스나 함수가 독립적으로 로직을 수행하고 필요한 데이터를 조회·갱신한다.
4. 비동기 이벤트, 보상 트랜잭션, 캐시 전략으로 분산 일관성 문제를 완화한다.
5. 관측성·재시도·회로 차단·배포 전략으로 장애 전파를 제어한다.

| 비교 | 모놀리식 | MSA |
|:---|:---|:---|
| **배포** | 전체 재배포 | **서비스별 독립** |
| **스케일링** | 전체 | **서비스별** |
| **복잡도** | 낮음 (초기) | **높음 (운영)** |
| **적합** | **소규모·초기** | 대규모·복잡 |

---

## 4. 비교 및 연결

### Monolith First 전략 (Martin Fowler)
1. 초기에는 모놀리식으로 빠르게 개발.
2. 도메인 경계가 명확해지면 서비스를 분리.
3. "처음부터 MSA"는 과잉 엔지니어링 위험.

---

## 5. 실무 적용 및 판단

모놀리식은 **MSA의 반대가 아닌 출발점**이며, 적절한 시점에 도메인 경계를 따라 서비스를 분리하는 것이 현실적 전략이다.

---

## 6. 기대효과 및 결론

모놀리식 아키텍처 (Monolithic Architecture) - 단일체 구조의 특징과 한계를 올바르게 적용하면 확장성, 운영 안정성, 자동화 수준, 가시성을 함께 높일 수 있다. 다만 이 효과는 기술 자체만으로 생기지 않으며, 책임 경계·표준화·보안 통제·비용 관리가 함께 설계될 때 비로소 현실화된다.

향후에는 관리형 서비스, 정책 자동화, AI 기반 운영 최적화와 결합되면서 모놀리식 아키텍처 (Monolithic Architecture) - 단일체 구조의 특징과 한계의 중요성이 더 커질 것이다. 따라서 핵심은 특정 도구를 도입하는 것이 아니라, 업무 특성과 조직 역량에 맞는 적용 범위와 운영 모델을 설계하는 데 있다.

---

## 7. 발전 흐름도

```text
[모놀리식 (전통, ~2010s)]
    │
    ▼
[SOA (Service Oriented Architecture, 2005~)]
    │
    ▼
[MSA (2014, Netflix·Amazon) — 서비스 분리]
    │
    ▼
[Modular Monolith (2020~) — 모놀리식 + 모듈 경계]
    │
    ▼
[현재: "Right-sizing" — 상황에 맞는 아키텍처 선택]
```

---

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| **모놀리식** | 단일 코드베이스·배포 단위 |
| **MSA** | 서비스별 독립 배포·스케일링 |
| **Modular Monolith** | 모놀리식 + 모듈 경계 (절충안) |
| **Monolith First** | Martin Fowler의 점진적 전환 전략 |
| **Strangler Fig** | 모놀리식→MSA 점진 마이그레이션 패턴 |

---
