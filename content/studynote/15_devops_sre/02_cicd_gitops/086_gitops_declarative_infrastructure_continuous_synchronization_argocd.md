+++
weight = 86
title = "86. GitOps (깃옵스)"
date = "2026-04-10"
[extra]
categories = "studynote-devops-sre"
+++

## 0. 핵심 인사이트

> **핵심**: GitOps (Git-based Operations)는 Git을 단일 진실 공급원으로 삼아 선언형 상태를 운영하는 방식이다.
> **비유**: 노트 한 장이 시스템의 기준이 된다.

---

> 📝 모범 답안

## 1. 개요 및 필요성

GitOps는 배포 명령이 아니라 원하는 상태 선언을 중심에 둔다. 사람이 서버에 직접 들어가 수정하는 대신, 저장소의 선언을 컨트롤러가 읽고 클러스터 상태를 맞춘다.

변경 이력, 리뷰, 승인, 롤백이 모두 Git 흐름 안에 남는 것이 가장 큰 장점이다.

---

## 2. 구성요소

| 구성 요소 | 역할 | 포인트 |
|:---|:---|:---|
| Git 저장소 | 원하는 상태의 원본 | PR 리뷰, 이력 추적 |
| 매니페스트 | 배포 선언 | 재현성 |
| 컨트롤러 | 선언과 실제를 비교 | 드리프트 감지 |
| Kubernetes 클러스터 | 실제 실행 환경 | 자동 동기화 |
| Argo CD | 대표적인 GitOps 도구 | 싱크와 헬스 체크 |

┌──────────┐ commit ┌───────────────┐ sync ┌───────────────┐
│   Git    │──────▶│   Argo CD     │─────▶│ Kubernetes    │
│  repo    │       │ controller    │      │ cluster       │
└────┬─────┘       └──────┬────────┘      └──────┬────────┘
     │ drift detect          │ actual state          │
     └──────────────────────◀─┴───────────────────────┘

---

## 3. 구조 및 원리

**핵심 조건**: 모든 변경이 버전 관리되고, 자동 검증을 통과하며, 배포 상태와 롤백 경로가 추적 가능해야 한다.

동작 순서:
1. 코드와 배포 정의를 저장소에 커밋해 변경 이력을 단일 진실 공급원으로 만든다.
2. 빌드·테스트·보안 검사를 자동 수행해 배포 가능 아티팩트를 만든다.
3. 환경별 설정을 분리하면서도 동일한 파이프라인으로 일관성을 유지한다.
4. 배포 후 메트릭과 로그를 확인하고 이상 시 즉시 롤백하거나 다음 단계 승인을 중단한다.

```text
소스 변경
   ↓
CI 검증
   ↓
아티팩트 저장
   ↓
CD/GitOps 동기화
   ↓
운영 반영·롤백
```

| 비교 항목 | GitOps | 수동 kubectl/apply | 전통적 CI/CD 푸시 |
|:---|:---|:---|:---|
| 진실의 원천 | Git | 사람 기억 | 파이프라인 결과 |
| 변경 방식 | 선언형 | 명령형 | 배포 명령 중심 |
| 감사/추적 | 매우 강함 | 약함 | 중간 |
| 드리프트 복구 | 자동 | 수동 | 부분 자동 |

GitOps는 IaC와 CI/CD를 묶되, 운영 기준을 Git에 고정한다.

---

## 4. 비교 및 연결

- [ ] 모든 배포 가능한 상태를 Git 저장소에 넣는다.
- [ ] PR 승인 없이는 운영 반영이 되지 않도록 한다.
- [ ] 비밀값은 별도 보안 저장소로 분리한다.
- [ ] 자동/수동 동기화 정책과 롤백 기준을 정한다.

- ❌ 운영 서버에 직접 들어가서 임시 수정하는 습관
- ❌ Git과 실제 클러스터가 서로 다른 두 진실이 되는 상태
- ❌ 비밀값을 평문으로 저장하는 것

---

## 5. 실무 적용 및 판단

GitOps는 사람의 실수 자체를 없애기보다, 실수가 생겨도 다시 맞출 수 있게 만든다. 그래서 배포는 빠르기만 한 과정이 아니라 복원 가능한 과정이 된다.

---

## 6. 기대효과 및 결론

GitOps (깃옵스)을(를) 도입하면 자동화와 표준화를 통해 속도·안정성·가시성의 균형을 높일 수 있다. 다만 효과를 얻으려면 모든 변경이 버전 관리되고, 자동 검증을 통과하며, 배포 상태와 롤백 경로가 추적 가능해야 한다는 전제가 충족되어야 하며, 도구만 도입하고 운영 원칙을 바꾸지 않으면 기대 효과가 빠르게 한계에 부딪힌다.

결론적으로 GitOps (깃옵스)은(는) 개별 기능이 아니라 운영 체계 전체의 품질을 재정렬하는 방식이며, 향후에는 플랫폼화·정책화·AI 기반 최적화와 결합해 더 높은 수준의 자동 운영으로 확장된다.

---

## 7. 발전 흐름도

```text
코드 커밋 → PR 리뷰 → 머지 → 동기화 → 드리프트 감지 → 실제 상태 재조정
```

---

## 8. 관련 개념 맵

| 개념 | 연결 포인트 |
|:---|:---|
| Git 저장소 | 원하는 상태를 보관하는 기준점이다. |
| Argo CD | 선언과 실제를 맞추는 조정자다. |
| 드리프트 | 실제 상태와 선언의 차이다. |
| 롤백 | 이전 선언으로 되돌리는 안전장치다. |
| IaC (Infrastructure as Code) | 인프라를 코드로 다루는 바탕이다. |
