import os
import unicodedata

def get_char_width(c):
    if c in '▶◀▼▲■': return 1
    if 0x2500 <= ord(c) <= 0x257F: return 1
    if unicodedata.east_asian_width(c) in ('W', 'F'): return 2
    return 1

def get_str_width(s):
    return sum(get_char_width(c) for c in s)

def align_ascii_box(text):
    lines = text.strip().split('\n')
    out = []
    in_box = False
    box_lines = []
    
    for line in lines:
        if line.startswith('```text'):
            out.append(line)
            in_box = True
        elif line.startswith('```') and in_box:
            # Process box_lines
            max_w = 0
            for bl in box_lines:
                if bl.startswith('┌') or bl.startswith('├') or bl.startswith('└') or bl.startswith('│'):
                    inner = bl[1:-1] if bl[-1] in '┐┤┘│' else bl[1:]
                    w = get_str_width(inner.rstrip('─ '))
                    if w > max_w: max_w = w
                else:
                    w = get_str_width(bl)
                    if w > max_w: max_w = w
            
            # 2 spaces padding minimum
            max_w += 2 

            for bl in box_lines:
                if bl.startswith('┌'):
                    out.append('┌' + '─' * max_w + '┐')
                elif bl.startswith('├'):
                    inner = bl[1:-1] if bl[-1] == '┤' else bl[1:]
                    inner = inner.strip('─')
                    if inner:
                        w = get_str_width(inner)
                        out.append('├' + inner + '─' * (max_w - w) + '┤')
                    else:
                        out.append('├' + '─' * max_w + '┤')
                elif bl.startswith('└'):
                    out.append('└' + '─' * max_w + '┘')
                elif bl.startswith('│'):
                    inner = bl[1:-1] if bl[-1] == '│' else bl[1:]
                    inner = inner.rstrip()
                    w = get_str_width(inner)
                    out.append('│' + inner + ' ' * (max_w - w) + '│')
                else:
                    out.append(bl)
            
            out.append(line)
            in_box = False
            box_lines = []
        elif in_box:
            box_lines.append(line)
        else:
            out.append(line)
    return '\n'.join(out) + '\n'

def write_file(filename, content):
    aligned_content = align_ascii_box(content)
    filepath = f"content/studynote/1_computer_architecture/3_architecture_basics_performance/{filename}"
    os.makedirs(os.path.dirname(filepath), exist_ok=True)
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(aligned_content)

c125 = """+++
title = "프로그램 내장 방식 (Stored Program Concept)"
weight = 125
+++

# 프로그램 내장 방식 (Stored Program Concept)

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 프로그램 내장 방식은 명령어(코드)를 데이터와 동일한 2진수 형태로 변환하여 컴퓨터 내부의 **주기억장치에 함께 저장**하고, 순차적으로 읽어와 실행하는 설계 사상이다.
> 2. **가치**: 배선을 직접 교체하던 하드와이어드(Hardwired) 방식을 타파하고, 메모리 속 데이터만 교체하면 어떤 작업이든 가능한 범용 소프트웨어 컴퓨터의 시대를 열었다.
> 3. **융합**: 이 원리는 폰 노이만 아키텍처의 근간이 되었으며, 프로그램 카운터(PC)의 제어 흐름에 따라 메모리에서 명령어를 인출(Fetch)하는 명령어 사이클의 본질적 이유가 된다.

---

## Ⅰ. 개요 및 필요성 (Context & Necessity)

- **개념**: 프로그램 내장 방식(Stored Program Concept)은 앨런 튜링의 '보편 튜링 기계' 이론을 존 폰 노이만이 실제 하드웨어로 구현하기 위해 제안한 개념이다. 연산 규칙을 담은 프로그램을 데이터와 섞어 임의 접근 메모리(RAM)에 보관한다.
- **필요성**: 초기 에니악(ENIAC)은 다른 연산을 수행하려면 사람이 며칠에 걸쳐 수천 개의 진공관 배선을 다시 꽂아야 했다. 하드웨어의 연산 속도는 빛에 가까웠지만, 프로그래밍 속도는 인간의 물리적 한계에 묶여 있었다. 이를 해결하기 위해 배선 대신 '저장 가능한 코드'가 필요했다.
- **💡 비유**: 붕어빵 기계(하드와이어드)는 붕어빵만 만들 수 있지만, 3D 프린터(프로그램 내장 방식)는 메모리 카드(프로그램)만 바꿔 끼우면 컵, 장난감, 부품 등 무엇이든 만들어낼 수 있는 것과 같습니다.

- **등장 배경**:
  1945년 발표된 에드박(EDVAC) 초안에서 폰 노이만은 명령어와 데이터를 동일한 메모리에 저장하는 혁신적 구조를 제안했다. 이후 이 방식을 적용한 최초의 컴퓨터 에드삭(EDSAC)이 1949년에 완성되며 소프트웨어라는 개념이 역사상 처음으로 하드웨어에서 분리 독립하게 되었다.

```text
┌────────────────────────────────────────────────────────┐
│     하드와이어드 프로그래밍 vs 프로그램 내장 방식 패러다임 비교    │
├────────────────────────────────────────────────────────┤
│                                                        │
│ [과거: 하드와이어드 (Hardwired) - 배선 교체 방식]              │
│                                                        │
│   사람 ──(전선 꽂기)──▶ [ 산술 논리 회로 (고정) ]               │
│   단점: 프로그램 변경 시 수일 소요, 극도의 비효율                  │
│                                                        │
│ [현대: 프로그램 내장 방식 (Stored Program)]                 │
│                                                        │
│   ┌────────────────────────────────────────────────┐   │
│   │                 주기억장치 (Memory)                │   │
│   │  ┌─────────────────┐ ┌──────────────────────┐  │   │
│   │  │ 명령어(코드) 영역  │ │ 데이터(변수) 영역         │  │   │
│   │  └─────────────────┘ └──────────────────────┘  │   │
│   └────────────────────────┬───────────────────────┘   │
│                 명령어 인출(Fetch) │                        │
│                            ▼                           │
│   ┌────────────────────────────────────────────────┐   │
│   │                중앙처리장치 (CPU)                  │   │
│   │       (명령어를 읽고, 해석하여, 하드웨어를 제어함)        │   │
│   └────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 핵심은 '명령어의 데이터화'이다. CPU는 내부에 고정된 덧셈, 뺄셈 회로를 가지고 있지만 스스로 무엇을 할지는 모른다. CPU는 단지 주기억장치에 저장된 0과 1의 비트 패턴(명령어)을 순서대로 읽어 들여(Fetch), 그 패턴에 따라 내부 스위치를 전자적으로 껐다 켰다 할 뿐이다. 이 구조 덕분에 메모리 내용만 소프트웨어적으로 덮어쓰면 컴퓨터는 어제는 계산기, 오늘은 타자기, 내일은 게임기로 무한히 변신할 수 있게 되었다.

- **📢 섹션 요약 비유**: 매번 다른 곡을 연주할 때마다 피아노 건반(하드웨어)을 뜯어고치는 대신, 피아노는 그대로 두고 피아니스트(CPU)에게 새로운 악보(프로그램)만 건네주는 완벽한 분업 시스템입니다.

---

## Ⅱ. 아키텍처 및 핵심 원리 (Deep Dive)

### 프로그램 내장 방식의 3대 핵심 메커니즘

| 핵심 메커니즘 | 상세 원리 | 파급 효과 |
|:---|:---|:---|
| **명령어의 데이터화** | 연산자(Opcode)와 피연산자(Operand)를 2진수 비트열로 인코딩하여 메모리에 저장. | 하드웨어를 물리적으로 변경하지 않아도 논리적 변경 가능 |
| **순차적 실행 제어** | 프로그램 카운터(PC)가 메모리 주소를 +1씩 증가시키며 다음 명령어를 자동으로 가리킴. | 명령어 사이클(Fetch-Decode-Execute)의 자동 반복 구현 |
| **분기(Branch) 기능** | JUMP, 조건문 명령어를 통해 PC의 값을 임의의 주소로 강제 변경함. | 반복문(Loop)과 조건 분기(If-Else)를 통한 알고리즘 흐름 통제 |

### 프로그램 카운터(PC)와 명령어 인출(Fetch)의 흐름
프로그램 내장 방식이 숨을 쉬게 하는 심장이 바로 '프로그램 카운터(PC)'다. 모든 프로그램 라인에는 메모리의 물리적 주소가 매핑된다.

```text
┌────────────────────────────────────────────────────────┐
│     프로그램 카운터(PC)를 통한 순차적 명령어 실행 메커니즘       │
├────────────────────────────────────────────────────────┤
│                                                        │
│    [ CPU 내부 ]                 [ 주기억장치 (Memory) ]       │
│                                                        │
│ ┌────────────────┐          주소(Addr)     저장된 값(명령/데이터)│
│ │ 프로그램 카운터(PC)│          ┌──────┐ ┌────────────────┐   │
│ │    [ 0x1000 ]  │──Fetch──▶│0x1000│ │ LOAD R1, A     │   │
│ └────────────────┘          │0x1004│ │ ADD R1, B      │   │
│                             │0x1008│ │ STORE R1, C    │   │
│ ┌────────────────┐          │0x100C│ │ JUMP 0x1000    │   │
│ │명령어 레지스터(IR) │          └──────┘ └────────────────┘   │
│ │ [LOAD R1, A]   │◀──반환─── 메모리에서 명령어를 읽어 CPU로 전송 │
│ └────────────────┘                                     │
│                                                        │
│  [동작 순서]                                             │
│  1. PC가 0x1000번지를 가리키고, 해당 주소의 명령어를 IR로 로드.    │
│  2. 명령어를 읽자마자 PC는 자동으로 0x1004로 증가 (순차 실행 준비).│
│  3. IR에 담긴 명령어를 해독(Decode)하고 실행(Execute).        │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** CPU는 스스로 생각을 하는 것이 아니라, 오직 PC(프로그램 카운터)가 가리키는 메모리 주소로 달려가 그곳에 있는 데이터를 무조건 '명령어'라고 믿고 가져와 실행한다. 만약 PC가 0x1000에서 시작해 0x100C의 JUMP 명령을 만나면 PC 값은 다시 0x1000으로 덮어써 지고 무한 루프가 형성된다. 이러한 주소 기반의 순차적 접근과 분기가 프로그램 내장 방식의 본질적인 실행 제어 흐름이다.

- **📢 섹션 요약 비유**: 오디오 테이프를 재생할 때, 헤드(PC)가 테이프(메모리)를 1초씩 순서대로 읽어나가다가 '되감기(JUMP)' 버튼을 누르면 다시 처음으로 돌아가 노래를 반복하는 원리와 같습니다.

---

## Ⅲ. 융합 비교 및 다각도 분석

### 튜링 머신 vs 프로그램 내장 방식

| 비교 관점 | 보편 튜링 머신 (Universal Turing Machine) | 프로그램 내장 방식 (Stored Program) |
|:---|:---|:---|
| **위상** | 수학적/이론적 컴퓨터 모델 | 튜링 머신의 물리적/공학적 구현 |
| **저장 매체** | 무한히 긴 1차원의 종이 테이프 | 유한한 용량의 임의 접근 메모리 (RAM) |
| **접근 방식** | 테이프 헤드를 왼쪽/오른쪽으로 1칸씩 순차 이동 | 고유 주소(Address)를 통해 O(1)에 직접 접근 |
| **명령어 취급** | 상태표(State Table)에 기호로 정의됨 | 0과 1의 전기적 비트 패턴으로 인코딩됨 |

앨런 튜링이 "모든 계산은 테이프 위의 기호를 조작하는 것으로 환원된다"고 수학적으로 증명했다면, 폰 노이만은 그 테이프를 빠른 전자적 메모리(RAM)로 대체하고, 헤드의 이동을 버스와 주소 지정(Addressing)으로 바꾼 공학적 승리를 거둔 것이다.

### 폰 노이만 병목 (Von Neumann Bottleneck)의 잉태
프로그램과 데이터를 한 곳(메모리)에 때려 넣으면서 치명적인 트레이드오프가 발생했다. CPU는 명령어를 읽을 때도 메모리에 가야 하고, 연산할 데이터를 읽을 때도 메모리에 가야 한다. 이로 인해 CPU와 메모리 사이의 단일 통로(버스)에 극심한 병목이 발생하며, 이를 '폰 노이만 병목'이라 부른다.

- **📢 섹션 요약 비유**: 수만 권의 책(데이터와 명령어)을 한 방에 모아둔 것은 혁신이었지만, 그 방으로 들어가는 문(버스)이 하나뿐이라서 천재 학자(CPU)가 문 앞에서 책이 나오기만 하염없이 기다리게 된 부작용을 낳았습니다.

---

## Ⅳ. 실무 적용 및 기술사적 판단

### 실무 시나리오 및 아키텍처 연계

1. **시나리오 — 운영체제의 프로세스 로딩**: 사용자가 아이콘을 클릭하여 앱을 실행할 때 시스템의 동작.
   - **의사결정**: 프로그램 내장 방식 하에서 하드디스크에 있는 프로그램은 그저 죽은 데이터 덩어리에 불과하다. OS의 로더(Loader)는 이 파일을 RAM으로 복사(Load)하고 메모리 주소를 할당한다. 그런 다음 CPU의 PC(프로그램 카운터)에 이 메모리 시작 주소를 강제로 덮어씌움으로써, 죽어있던 데이터가 살아 움직이는 '프로세스(Process)'로 전환된다.

2. **시나리오 — 스택 스매싱 (Stack Smashing) 해킹 방어**: 해커가 입력 폼에 엄청나게 긴 데이터를 밀어 넣어 메모리 구조를 덮어쓰고, 그 데이터 속에 숨겨둔 악성 코드를 실행시키려는 시도.
   - **의사결정**: 프로그램 내장 방식의 최대 취약점은 "CPU는 메모리에 있는 것이 데이터인지 명령어인지 구분하지 못하고 PC가 가리키면 무조건 실행한다"는 점이다. 이를 막기 위해 실무에서는 OS 레벨에서 DEP (Data Execution Prevention), 하드웨어 레벨에서 NX Bit (No-Execute Bit)를 도입하여, 메모리의 데이터 영역(Stack, Heap)에 있는 값은 절대 명령어로 실행(Execute)되지 못하게 하드웨어적으로 차단한다.

3. **시나리오 — JIT(Just-In-Time) 컴파일러 엔진**: 자바스크립트나 V8 엔진이 런타임에 동적으로 코드를 최적화하는 과정.
   - **의사결정**: 런타임 환경은 메모리 안에서 데이터(소스코드)를 읽어 즉석에서 최적화된 새로운 기계어(명령어)를 메모리 다른 곳에 생성해 낸 뒤 그곳으로 PC를 점프시킨다. "코드와 데이터는 본질적으로 같다"는 프로그램 내장 방식의 철학이 없었다면 현대의 동적 언어 최적화는 불가능했다.

### 안티패턴
- **자가 수정 코드 (Self-Modifying Code)**: 실행 중에 프로그램이 자신의 명령어 코드를 직접 수정하는 기법. 과거 메모리가 극도로 부족할 때는 썼지만, 현대에는 명령어 캐시(L1i)와 데이터 캐시(L1d)가 분리되어 있어 코드를 덮어쓰는 순간 캐시 코히런시가 깨지고 파이프라인이 붕괴하며 치명적 크래시를 유발하는 절대 금기 패턴이다.

- **📢 섹션 요약 비유**: 요리사(CPU)는 맹목적이라서 도마(메모리) 위에 있는 것이 고기(데이터)인지 돌멩이(악성코드)인지 보지 않고 무조건 칼질(실행)을 합니다. 따라서 주방장(OS)이 철저하게 도마에 올라갈 재료의 구역을 통제해야만 독극물 사고(해킹)를 막을 수 있습니다.

---

## Ⅴ. 기대효과 및 결론

### 기대효과 및 진화
- **소프트웨어 산업의 탄생**: 컴퓨터를 새로 사지 않아도 메모리에 다운로드하는 것만으로 기능을 바꿀 수 있게 되어, 구글, MS, 애플 같은 거대한 소프트웨어 생태계가 탄생할 수 있는 토양이 마련되었다.
- **아키텍처의 한계 돌파**: 프로그램 내장 방식이 낳은 병목 현상을 타파하기 위해, 명령어를 미리 가져오는 파이프라이닝(Pipelining), 자주 쓰는 명령어를 모아두는 캐시 메모리(Cache), 명령어와 데이터를 쪼개는 하버드 아키텍처 등 현대 프로세서의 눈부신 최적화 기술들이 파생되었다.

### 결론
프로그램 내장 방식은 단순히 하드웨어의 결선을 메모리로 옮긴 것이 아니다. 이는 인간의 논리적 사고(알고리즘)를 기계의 물리적 구속으로부터 해방시킨 가장 위대한 추상화다. 이 사상이 있었기에 컴퓨터는 단순한 계산기(Calculator)를 넘어 모든 것을 시뮬레이션할 수 있는 궁극의 가상 기계(Virtual Machine)로 진화했다.

- **📢 섹션 요약 비유**: 찰흙을 구워 만든 항아리(과거 컴퓨터)는 굳으면 모양을 바꿀 수 없지만, 프로그램 내장 방식은 레고 블록(0과 1)과 같아서 오늘 성을 만들고 내일 허물어 우주선을 만들 수 있는 창조의 혁명입니다.

---

## 📌 관련 개념 맵 (Knowledge Graph)

| 개념 명칭 | 관계 및 시너지 설명 |
|:---|:---|
| **폰 노이만 아키텍처 (Von Neumann)** | 프로그램 내장 방식을 제어, 연산, 기억, 입출력 구조에 결합하여 완성한 컴퓨터 하드웨어 아키텍처의 대명사. |
| **프로그램 카운터 (Program Counter, PC)** | 메모리에 내장된 프로그램 명령어 중 다음 번에 꺼내올 명령어의 주소를 보관하여 실행 흐름을 지휘하는 레지스터. |
| **명령어 사이클 (Instruction Cycle)** | 메모리에 저장된 프로그램을 인출(Fetch), 해독(Decode), 실행(Execute)하는 일련의 끊임없는 기계적 반복 흐름. |
| **폰 노이만 병목 (Von Neumann Bottleneck)** | 프로그램 코드와 데이터가 같은 메모리 버스를 타고 와야 해서 발생하는 극심한 교통 체증과 파이프라인 지연. |
| **하버드 아키텍처 (Harvard Architecture)** | 내장된 프로그램 영역과 데이터 영역의 통로(버스)를 둘로 쪼개어 병목을 해소하려 한 발전된 캐시 분리 구조. |

---

## 👶 어린이를 위한 3줄 비유 설명
1. 옛날 컴퓨터는 계산기를 다른 용도로 쓰려면 어른들이 전선을 다 뽑고 다시 꽂아야 해서 하루 종일 걸렸어요.
2. 어떤 천재 할아버지가 "게임하는 방법(프로그램)을 그냥 글자로 써서 서랍(메모리)에 넣어두고 컴퓨터가 직접 읽게 하자!"라고 발명했어요.
3. 이 발명 덕분에 우리는 선을 뽑을 필요 없이, 그냥 화면에서 아이콘만 클릭하면 컴퓨터가 서랍에서 알아서 게임을 꺼내 실행해 주는 마법 같은 세상에 살게 되었답니다!
"""

c126 = """+++
title = "하버드 아키텍처 (Harvard Architecture)"
weight = 126
+++

# 하버드 아키텍처 (Harvard Architecture)

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 하버드 아키텍처 (Harvard Architecture)는 명령어(프로그램 코드)를 저장하는 메모리와 연산할 데이터(변수)를 저장하는 **메모리를 물리적으로 완전히 분리하고, 각각 독립적인 버스(Bus)를 연결**한 컴퓨터 구조 설계다.
> 2. **가치**: 폰 노이만 아키텍처의 최대 약점인 단일 버스 병목(명령어와 데이터를 동시에 가져올 수 없는 문제)을 해결하여, 1클럭 사이클 내에 명령어 인출과 데이터 접근을 동시에 수행하는 극단적 병렬 처리를 가능하게 했다.
> 3. **융합**: 순수 하버드 아키텍처는 배선이 너무 복잡해 메인 메모리에 쓰기 어렵지만, 현대 CPU 내부의 L1 캐시(명령어 캐시와 데이터 캐시의 분리)에 적용되는 **모디파이드 하버드 아키텍처(Modified Harvard)**의 형태로 융합되어 프로세서 성능 향상의 표준이 되었다.

---

## Ⅰ. 개요 및 필요성 (Context & Necessity)

- **개념**: 하버드 아키텍처는 프로그램 메모리와 데이터 메모리가 물리적으로 분리되어 있으며, 주소 버스와 데이터 버스 역시 명령어용과 데이터용으로 각각 2세트씩 존재하는 버스 분리형 컴퓨터 아키텍처다.
- **필요성**: 폰 노이만 구조는 명령어와 데이터를 하나의 메모리와 버스로 공유하기 때문에 자원 충돌이 일어난다. CPU가 파이프라인을 돌려 1초에 수십억 번 명령을 처리하려 해도, 메모리에서 명령어를 가져오는 동안에는 데이터를 가져올 수 없어 CPU가 정지(Stall)하는 '폰 노이만 병목'이 발생했다. 속도를 극한으로 끌어올리려면 길이 하나가 아니라 두 개가 필요했다.
- **💡 비유**: 주방장(CPU)이 냉장고 1대(폰 노이만)에서 레시피(명령어)와 식재료(데이터)를 번갈아 꺼내느라 문을 열고 닫기를 반복하다가 지쳐버리자, 아예 고기 전용 냉장고와 야채 전용 냉장고를 양옆에 두고 양손으로 동시에 꺼내는 쾌속 주방을 만든 것과 같습니다.

- **등장 배경**:
  이 구조는 1944년 하버드 대학교의 하워드 에이컨(Howard Aiken)이 개발한 'Harvard Mark I' 릴레이 컴퓨터에서 유래했다. 당시에는 천공 카드(명령어)와 전기기계식 카운터(데이터)를 아예 다른 물리적 장치로 구성했다. 이후 DSP(디지털 신호 처리기)와 마이크로컨트롤러(MCU)에서 1클럭당 1연산을 무조건 보장해야 하는 엄격한 실시간성(Real-time)을 위해 이 사상이 부활했다.

```text
┌────────────────────────────────────────────────────────┐
│           하버드 아키텍처의 버스 분리형 구조 도식도            │
├────────────────────────────────────────────────────────┤
│                                                        │
│   ┌────────────────────────┐    ┌──────────────────┐   │
│   │ 명령어 메모리 (Inst Mem) │    │ 데이터 메모리 (Data)│   │
│   │  (ex. Flash, ROM)      │    │  (ex. SRAM, RAM) │   │
│   └────┬────────────▲──────┘    └────┬────────▲────┘   │
│        │ 명령어      │명령어            │ 데이터   │데이터  │
│        │ 데이터 버스  │주소 버스          │ 버스     │주소 버스│
│        ▼            │                 ▼        │      │
│   ┌────────────────────────────────────────────────┐   │
│   │                 중앙처리장치 (CPU)                 │   │
│   │                                                │   │
│   │       [명령어 인출 유닛]          [데이터 적재 유닛]     │   │
│   │       (Instruction Fetch)     (Data Load/Store)│   │
│   └────────────────────────────────────────────────┘   │
│                                                        │
│  * 특징: 명령어를 가져오면서(Fetch), 동시에 앞선 명령어의     │
│          데이터를 읽고 쓸 수 있다(Memory Access). 완벽한 병렬. │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 그림에서 가장 눈여겨볼 점은 CPU와 메모리 사이를 잇는 화살표(버스)가 2배로 늘어났다는 점이다. 왼쪽의 명령어 메모리는 주로 코드가 지워지지 않는 Flash나 ROM으로 구성되고, 오른쪽 데이터 메모리는 읽고 쓰기가 빠른 SRAM으로 구성된다. 통로가 2개이므로, 파이프라인에서 현재 명령어가 데이터를 계산해 SRAM에 쓰는 동안, CPU는 지연 없이 바로 다음 명령어를 Flash에서 가져올 수 있다. 구조적 해저드(Structural Hazard)가 하드웨어적으로 원천 봉쇄되는 구조다.

- **📢 섹션 요약 비유**: 왕복 2차선 도로(폰 노이만)에서는 화물차와 승용차가 서로 양보하며 다녀야 하지만, 하버드 아키텍처는 아예 화물차 전용 고속도로와 승용차 전용 고속도로를 따로 깔아버려 막힘없는 무한 질주를 가능하게 한 도로망입니다.

---

## Ⅱ. 아키텍처 및 핵심 원리 (Deep Dive)

### 하버드 아키텍처의 동작 원리와 대역폭(Bandwidth) 이점

하버드 아키텍처의 가장 큰 무기는 버스 폭(Bus Width)을 명령어와 데이터의 성격에 맞게 서로 다르게 설계할 수 있다는 점이다.
폰 노이만 구조에서는 버스가 32비트라면 명령어도 32비트, 데이터도 32비트로 규격이 강제된다. 반면 하버드 구조(특히 PIC, AVR 같은 MCU)에서는 **데이터 버스는 8비트로 작게 만들고, 명령어 버스는 14비트나 16비트로 넓게 만들어** 명령어 인출 효율을 극대화할 수 있다.

### 명령어 파이프라인 충돌 해소 메커니즘

파이프라인 5단계(IF-ID-EX-MEM-WB)에서 폰 노이만 구조는 IF(명령어 인출)와 MEM(메모리 데이터 접근)이 충돌한다. 하버드 구조는 이를 분리함으로써 파이프라인 정체(Stall)를 없앤다.

```text
┌────────────────────────────────────────────────────────┐
│     폰 노이만 vs 하버드 아키텍처 파이프라인 병렬성 비교 분석     │
├────────────────────────────────────────────────────────┤
│                                                        │
│  [폰 노이만 병목 발생: 메모리가 1개일 때]                    │
│   Cycle:  1     2     3     4     5     6              │
│   명령 1: [IF ] [ID ] [EX ] [MEM] [WB ]                │
│                              ▲ 충돌!                    │
│   명령 4:                   [IF ] [ID ] [EX ]          │
│                            (대기)                      │
│                                                        │
│  [하버드 아키텍처: 명령어/데이터 메모리 2개일 때]              │
│   Cycle:  1     2     3     4     5     6              │
│   명령 1: [IF ] [ID ] [EX ] [MEM] [WB ]                │
│                              │ (데이터 버스 사용)        │
│   명령 4:             [IF ] [ID ] [EX ] [MEM]          │
│                        │ (명령어 버스 동시 사용)          │
│                                                        │
│  결과: 충돌 없이 매 클럭마다 명령어 실행이 가능한 CPI=1 달성!   │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 상단의 폰 노이만 파이프라인은 Cycle 4에서 1번 명령어의 데이터 메모리 접근(MEM)과 4번 명령어의 코드 인출(IF)이 단일 버스 위에서 정면 충돌한다. 이 때문에 파이프라인에 기포(Bubble)가 생기고 속도가 꺾인다. 하단 하버드 구조에서는 MEM 단계는 데이터 버스를, IF 단계는 명령어 버스를 각자 사용하므로 둘이 완벽하게 겹쳐서 실행된다. 이것이 DSP(디지털 신호 처리기)가 오디오 신호를 1사이클의 지연도 없이 실시간으로 계산해낼 수 있는 마법의 근원이다.

- **📢 섹션 요약 비유**: 은행 창구가 하나뿐이면 입금 손님과 대출 손님이 한 줄로 서서 싸워야 하지만, 하버드 아키텍처는 입금 전용 창구와 대출 전용 창구를 완전히 나누어 대기표 없이 즉시 업무를 처리하는 완벽한 은행 시스템입니다.

---

## Ⅲ. 융합 비교 및 다각도 분석

### 폰 노이만 구조와 하버드 구조의 심층 비교

| 비교 항목 | 폰 노이만 아키텍처 | 순수 하버드 아키텍처 |
|:---|:---|:---|
| **메모리 구성** | 통합 메모리 (명령어 + 데이터) | 분리 메모리 (Inst Memory, Data Memory) |
| **버스(Bus) 수** | 1세트 (데이터/주소/제어 버스) | 2세트 (버스 회선 수 2배 증가) |
| **공간 활용도** | 프로그램/데이터 비율에 따라 유연하게 할당 | 영역이 고정되어 있어 한쪽 메모리 낭비 가능성 |
| **제조 비용/면적** | 핀(Pin) 수가 적고 라우팅이 단순해 저렴함 | 핀 수가 많고 회로 기판 라우팅이 복잡해 고비용 |
| **보안성** | 악성코드를 데이터로 위장 실행 취약 | 명령어 영역은 쓰기 불가로 보호되어 보안 우수 |

### 모디파이드 하버드 아키텍처 (Modified Harvard Architecture)의 탄생
순수 하버드 아키텍처는 배선이 너무 많아져(예: 64비트 버스 2세트면 핀만 수백 개 추가) 메인보드 밖으로 빼기엔 물리적으로 불가능에 가깝다. 그래서 현대 인텔 x86, ARM 코어는 두 아키텍처의 장점만 융합한 타협안을 만들었다.

- **CPU 외부 (메인 메모리 RAM)**: 폰 노이만 구조 (단일 버스, 비용 절감, 유연한 공간)
- **CPU 내부 (L1 캐시 메모리)**: 하버드 구조 (명령어 캐시 `L1i`와 데이터 캐시 `L1d` 분리, 병목 해소)

이 혼합 구조를 통해, 램에서 데이터를 덩어리로 가져올 때는 폰 노이만의 경제성을 챙기고, 실제 코어 내부에서 숨 가쁘게 연산할 때는 하버드의 병렬성을 누리는 궁극의 아키텍처가 완성되었다.

- **📢 섹션 요약 비유**: 도시와 도시 사이를 잇는 국도(메인 메모리)는 하나로 만들어 다리 건설 비용을 아끼고, 톨게이트를 지나 도심(CPU 코어)으로 진입하면 즉시 상행선과 하행선(명령어/데이터 캐시)을 입체 교차로로 철저히 분리하는 가장 현명한 도로 교통망 설계입니다.

---

## Ⅳ. 실무 적용 및 기술사적 판단

### 실무 시나리오 및 아키텍처 의사결정

1. **시나리오 — 항공기 제어용 MCU (마이크로컨트롤러) 선정**: 0.001초의 딜레이라도 생기면 드론이 추락하는 초정밀 실시간 제어(Hard Real-time) 시스템 보드 설계.
   - **의사결정**: 예측 불가능한 캐시 미스(Cache Miss)나 버스 지연이 발생하는 폰 노이만 기반의 일반 모바일 AP(예: Cortex-A)를 배제한다. 대신 Flash ROM(명령어)과 SRAM(데이터)이 완전히 분리된 순수 하버드 아키텍처 기반의 MCU(예: AVR, Cortex-M3)를 채택하여, 인터럽트 발생 시 정확히 예측된 클럭 사이클 내에 분기 및 데이터 처리가 100% 보장되도록 하드웨어를 설계한다.

2. **시나리오 — 고성능 L1 캐시 최적화 프로그래밍**: C++로 작성된 대규모 행렬 곱셈 연산에서 CPU 사용률은 높은데 실제 IPC(클럭당 명령어 처리 수)가 바닥을 치는 성능 저하 발생.
   - **의사결정**: 모디파이드 하버드 아키텍처의 특성을 이해해야 한다. 루프 문이 너무 길어 명령어 캐시(L1i) 범위를 초과하면 지속적인 L1i 미스가 발생해 데이터 캐시(L1d)가 아무리 완벽해도 파이프라인이 정지된다. 코드를 잘게 쪼개는 루프 분할(Loop Fission)이나 인라인(Inline) 함수 남용을 줄여 명령어 풋프린트(Code Footprint)를 L1i 캐시 크기(보통 32KB) 안에 욱여넣는 최적화를 수행한다.

3. **시나리오 — 펌웨어 보안 무결성 확보**: IoT 디바이스에서 버퍼 오버플로우를 통한 임의 코드 실행 해킹 방어.
   - **의사결정**: 하버드 아키텍처 MCU는 데이터 메모리(RAM)와 명령어 메모리(Flash)의 물리적 주소 체계 자체가 다르다. 해커가 RAM에 악성 코드를 밀어 넣고 그쪽으로 PC(프로그램 카운터)를 돌리더라도, 하드웨어는 RAM 영역을 '명령어 버스'로 읽을 수 있는 물리적 배선 자체가 존재하지 않으므로 구조적으로 해킹 코드가 실행되는 것을 완벽히 방어한다.

### 안티패턴
- **통합 캐시(Unified Cache)를 L1에 강제 설계**: 아주 저전력을 타겟으로 면적을 극단적으로 줄이려고 CPU 바로 옆의 최상위 L1 캐시마저 명령어/데이터 통합 캐시로 만들어버리는 설계. 이는 파이프라인 해저드를 증폭시켜 클럭 스피드를 올려도 성능이 오르지 않는 치명적인 폰 노이만 병목의 함정에 스스로 걸어 들어가는 최악의 아키텍처 벤치마크 결과를 초래한다.

- **📢 섹션 요약 비유**: 레이싱카(MCU)를 만들 때는 연비(공간 효율성)를 포기하더라도 엔진용 연료관과 냉각수 관(하버드 구조)을 완전히 따로 굵게 빼야 폭발적인 터보 스피드를 견딜 수 있습니다.

---

## Ⅴ. 기대효과 및 결론

### 기대효과 및 미래 전망
- **슈퍼스칼라와 비순차 실행의 토대**: 프로세서가 한 클럭에 여러 명령어를 동시에 쏘아 보내는 슈퍼스칼라(Superscalar) 기술은 명령어를 지연 없이 끊임없이 퍼 올릴 수 있는 넓고 분리된 하버드 버스 구조(L1i 캐시)가 전제되지 않으면 굶어 죽고 만다.
- **가속기(Accelerator)에서의 분리 아키텍처 고도화**: 최근 AI 추론을 위한 NPU나 텐서 코어 설계에서는, 가중치(Weight) 파라미터 전용 버스와 입력 활성화(Activation) 데이터 전용 버스를 완전히 분리하는 다중 하버드 아키텍처 모델을 채택하여 딥러닝 행렬 연산의 병목을 극한으로 타파하고 있다.

### 결론
하버드 아키텍처는 "모든 것은 데이터다"라는 폰 노이만의 우아한 추상화를 과감히 버리고, "명령어와 데이터는 태생부터 다르니 길도 달라야 한다"는 공학적 실용주의를 택한 걸작이다. 이 두 위대한 아키텍처의 타협물인 '모디파이드 하버드 구조'는 오늘날 지구상의 모든 고성능 스마트폰과 서버를 구동하는 보이지 않는 황금률이 되었다.

- **📢 섹션 요약 비유**: 이성(명령어)과 감성(데이터)이 섞여 갈등하던 하나의 뇌(폰 노이만)를, 철저한 좌뇌와 우뇌(하버드 아키텍처)로 분리하고 이를 뇌량(모디파이드)으로 연결함으로써 인류는 가장 빠르고 효율적인 전자 두뇌를 갖게 되었습니다.

---

## 📌 관련 개념 맵 (Knowledge Graph)

| 개념 명칭 | 관계 및 시너지 설명 |
|:---|:---|
| **폰 노이만 아키텍처 (Von Neumann)** | 하버드 구조와 대척점에 있는 통합 버스 모델로, 현대 메인 메모리의 기반 아키텍처. |
| **모디파이드 하버드 (Modified Harvard)** | 캐시는 하버드 구조로 분리하고 메인 메모리는 폰 노이만 구조로 통합한 현대 프로세서의 하이브리드 진화 형태. |
| **구조적 해저드 (Structural Hazard)** | 파이프라인에서 두 명령어가 동시에 단일 메모리 자원에 접근하려 할 때 발생하는 충돌로, 하버드 구조가 이를 근본적으로 해결함. |
| **디지털 신호 처리기 (DSP)** | 실시간 오디오/비전 처리를 위해 명령어와 데이터 동시 접근이 필수적이어서 하버드 구조를 극단적으로 채택한 칩. |
| **캐시 메모리 (L1i / L1d Cache)** | 현대 CPU 내부에서 하버드 아키텍처의 물리적 분리를 가장 완벽하게 시현하고 있는 1차 캐시의 두 영역. |

---

## 👶 어린이를 위한 3줄 비유 설명
1. 폰 노이만 컴퓨터는 장난감 상자 하나에 로봇 부품(데이터)과 조립 설명서(명령어)를 다 섞어놔서, 설명서를 읽으려면 부품을 잠시 내려놔야 했어요.
2. 하지만 하버드 컴퓨터는 "부품 상자"와 "설명서 상자"를 아예 따로따로 2개 만들었어요.
3. 덕분에 양손을 써서 왼손으로는 설명서를 계속 넘기며 읽고, 오른손으로는 쉴 새 없이 부품을 조립할 수 있어서 속도가 엄청나게 빨라진 거랍니다!
"""

c127 = """+++
title = "시스템 버스 (System Bus)"
weight = 127
+++

# 시스템 버스 (System Bus)

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 시스템 버스 (System Bus)는 컴퓨터의 4대 구성요소인 CPU, 메인 메모리, 입출력 장치들을 전자적으로 연결하여 데이터, 주소, 제어 신호를 실어 나르는 **공용 하드웨어 통신 고속도로**다.
> 2. **가치**: 수많은 컴퓨터 부품들이 각자 1:1로 선을 연결(Mesh)하지 않고, 하나의 공용 도로(Bus)에 버스 중재(Arbitration) 규칙을 통해 연결됨으로써 컴퓨터 구조의 확장성과 부품의 표준화(플러그 앤 플레이)를 가능하게 했다.
> 3. **융합**: CPU 클럭 속도 대비 버스의 전송 대역폭 한계로 인한 폰 노이만 병목을 극복하기 위해, 과거 단일 버스 구조에서 브리지(Bridge)를 통한 다중 계층 버스(FSB, BSB, PCIe) 구조로 진화하며 현대 칩셋 아키텍처의 핵심 뼈대가 되었다.

---

## Ⅰ. 개요 및 필요성 (Context & Necessity)

- **개념**: 버스(Bus)란 여러 하드웨어 모듈이 공통으로 접근하여 신호를 주고받을 수 있는 다중 점(Multi-drop) 연결 배선 시스템이다. 그중에서도 CPU와 메모리 등 핵심 컴포넌트를 직접 연결하는 최상위 버스를 시스템 버스라 부른다.
- **필요성**: 만약 컴퓨터 안의 CPU, 메모리, 하드디스크, 그래픽카드 10개 부품이 서로 통신하기 위해 각자 1:1 전용선을 깔아야 한다면, 전선의 수식은 $N(N-1)/2$가 되어 메인보드는 전선으로 뒤덮여 폭발할 것이다. 이를 해결하기 위해 "커다란 파이프라인(도로) 하나만 뚫어놓고, 모든 부품이 이 도로에 정류장처럼 접속해서 순서를 지키며 데이터를 주고받자"는 버스 토폴로지(Bus Topology)가 필수적으로 대두되었다.
- **💡 비유**: 대도시의 모든 집집마다 1:1로 개인용 지하철 터널을 뚫는 대신, 도시를 관통하는 거대한 지하철 노선(시스템 버스) 하나를 만들고, 모든 집과 회사(부품들)가 역을 통해 이 노선을 공유하는 것과 완벽히 같습니다.

- **등장 배경**:
  초창기 미니컴퓨터 시절 DEC의 PDP-8이 유니버스(Unibus)라는 단일 공용 버스 개념을 도입해 폭발적 성공을 거두었다. 이후 IBM PC 호환 기종의 ISA, PCI 등을 거치며 하드웨어를 꽂기만 하면 동작하는 표준화된 버스 인터페이스 슬롯 설계가 PC 대중화의 1등 공신이 되었다.

```text
┌────────────────────────────────────────────────────────┐
│           시스템 버스의 3대 기능적 논리 분할 구조 도식도          │
├────────────────────────────────────────────────────────┤
│                                                        │
│   ┌───────┐            ┌─────────┐            ┌───────┐│
│   │  CPU  │            │ Memory  │            │  I/O  ││
│   └──┬─┬─┬─┘            └───┬─┬─┬─┘            └──┬─┬─┬─┘│
│      │ │ │                  │ │ │                 │ │ │ │
│      │ │ └─[ 제어 버스 (Control Bus : R/W, ACK) ]───┴─┴─┴─┤
│      │ │                                                  │
│      │ └───[ 주소 버스 (Address Bus : Memory Location) ]──┤
│      │                                                    │
│      └─────[ 데이터 버스 (Data Bus : 실제 데이터 0/1) ]──────┤
│                                                        │
│  * 제어 버스: 단방향 또는 양방향 (누가 언제 쓸지 신호등 역할)          │
│  * 주소 버스: CPU ─▶ Memory 단방향 (목적지 번지수 지정)            │
│  * 데이터 버스: CPU ◀─▶ Memory 양방향 (실제 짐차 이동)             │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 물리적으로는 한 뭉치의 구리 배선이지만, 논리적으로 버스는 세 가닥으로 나뉜다. CPU가 메모리의 데이터를 읽고 싶을 때 동작 순서는 다음과 같다. ① 주소 버스에 원하는 번지수(예: 0x1A)를 올린다. ② 제어 버스에 'Read(읽기)' 신호를 킨다. ③ 메모리가 이를 인지하고 해당 번지의 데이터를 꺼내 데이터 버스에 올린다. ④ CPU가 데이터 버스를 통해 값을 수신한다. 이 세 버스의 폭(비트 수)이 컴퓨터의 궁극적 스펙을 결정한다. 주소 버스가 32비트면 메모리를 4GB($2^{32}$)밖에 달지 못하고, 데이터 버스가 64비트면 한 번에 8바이트의 데이터를 나를 수 있다.

- **📢 섹션 요약 비유**: 택배를 보낼 때 수령지 주소가 적힌 송장(주소 버스), 물건이 파손되지 않게 조심하라는 주의 스티커(제어 버스), 그리고 실제 상자 안의 물건(데이터 버스)이 하나의 트럭에 실려 이동하는 3박자 배송 시스템입니다.

---

## Ⅱ. 아키텍처 및 핵심 원리 (Deep Dive)

### 버스의 3대 분류와 폭(Width)의 의미

| 버스 종류 | 기능과 역할 | 대역폭/성능의 직결 요소 | 비유 |
|:---|:---|:---|:---|
| **데이터 버스 (Data Bus)** | CPU와 메모리/I/O 간 실제 명령어와 데이터를 전송하는 양방향 통로 | 워드(Word)의 크기를 결정. 넓을수록 한 번에 퍼 나르는 양(Throughput) 증가 | 트럭의 적재함 크기 |
| **주소 버스 (Address Bus)** | CPU가 접근할 목적지(메모리 번지나 I/O 포트 번호)를 알리는 단방향 통로 | 주소 버스 폭 = CPU가 장착 가능한 최대 메모리 용량 ($2^N$ Byte) | 내비게이션의 최대 목적지 수 |
| **제어 버스 (Control Bus)** | Read/Write, Interrupt, ACK, Bus Request 등 상태 제어 신호 전송 | 버스 경합(충돌)을 막는 중재 프로토콜 속도와 시스템 안정성 | 교차로의 신호등과 경찰관 |

### 다중 계층 버스 아키텍처 (Hierarchical Bus Architecture)
초기에는 단일 버스 하나에 CPU, 램, 하드디스크를 다 물렸다. 그러자 느린 하드디스크가 버스를 점유하는 동안 초고속 CPU가 하루 종일 멈춰 서는 재앙이 발생했다. 현대 시스템은 노스브리지(Northbridge)와 사우스브리지(Southbridge) 개념을 통해 **속도별로 고속도로를 분리하는 다중 계층 구조**를 띤다.

```text
┌────────────────────────────────────────────────────────┐
│           현대 PC의 다중 계층 버스 아키텍처 (계층 분리)            │
├────────────────────────────────────────────────────────┤
│                                                        │
│    ┌──────────┐   (BSB: Back-Side Bus, 수백 GB/s)      │
│    │ L2 Cache │◀──────────────────────────────────┐      │
│    └──────────┘                                  ▼      │
│ ┌────────────────┐  (FSB: Front-Side Bus)  ┌─────────┐ │
│ │   CPU Core   │◀────────고속 버스────────▶│ Main    │ │
│ └────────────────┘                         │ Memory  │ │
│         ▲                                  └─────────┘ │
│         │ (고속 PCIe Bus)                             │
│         ▼                                             │
│ ┌────────────────┐      (PCI / USB / SATA 저속 버스)    │
│ │  I/O Bridge  │◀────────저속 버스────────▶[ HDD,   │ │
│ │ (디바이스 제어기)│                          Keyboard ]│ │
│ └────────────────┘                                     │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 도로를 차급에 따라 분리한 설계다. 가장 최상위의 CPU와 L2/L3 캐시를 연결하는 BSB(후면 버스)는 빛의 속도로 동작한다. 그 아래 CPU와 메인 메모리를 연결하는 FSB(전면 버스)는 고속도로다. 그리고 키보드나 마우스 같은 거북이 장치들은 브리지(다리) 너머의 저속 로컬 버스(USB 등)로 완전히 격리시킨다. 느린 장치가 데이터를 보낼 때는 브리지가 버퍼링을 해두었다가 버스가 텅 빌 때 고속도로로 쏴줌으로써, 거북이 장치 때문에 페라리(CPU)가 급정거하는 병목(Bottleneck) 현상을 하드웨어적으로 차단한다.

- **📢 섹션 요약 비유**: 가장 빠른 KTX 전용선(FSB), 일반 무궁화호 노선(PCIe), 화물 열차 노선(SATA)을 환승역(브리지)으로 철저히 분리하여 똥차 때문에 스포츠카가 막히는 일을 원천 차단한 교통망 설계입니다.

---

## Ⅲ. 융합 비교 및 다각도 분석

### 동기식 버스 (Synchronous Bus) vs 비동기식 버스 (Asynchronous Bus)

버스를 이용하는 장치들이 통신 타이밍을 어떻게 맞추느냐에 따라 버스 프로토콜은 두 가지로 나뉜다.

| 비교 항목 | 동기식 버스 (Synchronous) | 비동기식 버스 (Asynchronous) |
|:---|:---|:---|
| **통신 기준** | 공통 클럭 발생기(Clock)의 주기적 신호(에지) | 클럭 없음. 장치 간 핸드셰이킹(Handshaking, REQ/ACK) 방식 |
| **속도 통제** | 시스템 내 가장 느린 장치에 클럭을 맞춰야 함 | 각 장치의 고유 속도로 유연하게 주고받음 |
| **장점** | 설계가 극도로 단순하고 클럭 단위 예측이 쉬움 | 이기종 장치 결합 자유로움, 버스 지연 최소화 |
| **단점** | 느린 장치 접속 시 고속 장치가 대기(Stall) 사이클 발생 | 핸드셰이크 프로토콜 오버헤드로 배선 복잡도 상승 |
| **주 사용처** | CPU-메모리 간 고속 로컬 버스 | 입출력 버스, 서로 다른 보드 간 통신 |

### 병렬 버스 (Parallel) vs 직렬 버스 (Serial) 의 역사적 역전 현상
과거에는 선이 여러 개면 무조건 빠르다고 믿었다. 32개의 선으로 32비트를 한 번에 보내는 병렬 버스(예: PCI, PATA)가 대세였다. 하지만 클럭 스피드가 GHz 단위로 올라가자, 32개의 선에서 신호가 도착하는 미세한 시간 차이(Clock Skew)와 선들끼리의 전자기 간섭(Crosstalk)이 발생해 에러가 폭증했다. 
결국 현대의 버스는 **무수히 많은 병렬 선을 버리고, 선 한두 개로 빛의 속도로 쏘아버리는 고속 직렬 버스 (Serial Bus - PCIe, SATA, USB 3.0)** 방식으로 완전히 진화하여 폰 노이만 병목을 새로운 차원에서 돌파했다.

- **📢 섹션 요약 비유**: 32명이 어깨동무를 하고 전력 질주(병렬 버스)를 하면 서로 발이 꼬여 넘어지기 쉽지만, 1명의 우사인 볼트(직렬 버스)가 편지 32장을 들고 음속으로 왕복 달리기하는 것이 최신 반도체 시대에는 훨씬 오류 없이 빠른 것과 같습니다.

---

## Ⅳ. 실무 적용 및 기술사적 판단

### 실무 시나리오 및 아키텍처 의사결정

1. **시나리오 — 고성능 GPU 추가 시 메인보드 병목 분석**: AI 연구소에서 최상위 그래픽카드(RTX 4090) 4개를 꽂았으나 연산 속도가 기대치의 절반밖에 안 나오는 상황.
   - **의사결정**: 메인보드의 PCIe 버스 레인(Lane) 분배 한계를 간과한 설계 오류다. CPU가 지원하는 총 PCIe 레인이 24개인데, GPU 1개당 x16 레인이 필요하므로 대역폭이 강제로 x8이나 x4로 반토막 나버린 것이다. 시스템 버스 아키텍처를 이해하여, 충분한 PCIe 레인(예: 128 레인)을 지원하는 서버용 워크스테이션 CPU(Threadripper, Xeon)로 시스템 보드를 전면 교체해야 버스 병목이 해소된다.

2. **시나리오 — 메모리 용량 한계 (32비트 OS의 비극)**: 구형 32비트 임베디드 운영체제에 8GB 램을 꽂았으나 4GB밖에 인식하지 못하는 현상.
   - **의사결정**: 하드웨어 프로세서의 주소 버스(Address Bus) 폭이 32비트라면, 메모리의 고유 번지를 $2^{32} = 4,294,967,296$개 (즉 4GB)까지만 물리적으로 표현할 수 있다. 남은 4GB는 길이 끊어져 갈 수가 없다. 이를 뚫기 위해 주소 버스 폭을 64비트로 늘린 새로운 아키텍처(x86-64)를 사용하거나, 임베디드에서는 레지스터 윈도우를 이용한 PAE(Physical Address Extension) 페이징 기법을 적용해 억지로 주소 공간을 넓혀야 한다.

3. **시나리오 — 버스 중재 (Bus Arbitration) 충돌**: 산업용 제어 장비에서 센서 모듈과 네트워크 카드가 동시에 버스 사용 권한을 요청(Bus Request)하여 충돌 발생.
   - **의사결정**: 여러 장치가 하나의 공용 버스를 사용할 때는 신호등 역할이 필수다. 버스 마스터(중재기)에 우선순위 부여 알고리즘을 도입한다. 생명과 직결된 센서 모듈에는 절대적 우선순위(Daisy Chain 방식 등 앞쪽 배치)를 주고, 네트워크 카드는 버스가 쉴 때 틈틈이 사용하도록 라운드 로빈(Round Robin) 중재 방식을 설정하여 충돌을 막는다.

### 안티패턴
- **마스터가 CPU뿐인 버스 설계 (Non-DMA 구조)**: 디스크에서 1GB 데이터를 메모리로 옮길 때, CPU가 버스의 주도권(마스터)을 독점하고 자기가 직접 한 바이트씩 옮기는 구조. 이는 "비싼 셰프가 요리(연산)는 안 하고 하루 종일 창고에서 배추를 직접 나르는 꼴"로 최악의 CPU 낭비다. 반드시 주변장치가 직접 메모리 버스를 통제하는 **DMA (Direct Memory Access) 컨트롤러**를 도입해 CPU는 연산만 하도록 버스 통제권을 이양해야 한다.

- **📢 섹션 요약 비유**: 도로(버스)는 막히면 무조건 끝입니다. 자동차 엔진(CPU)을 10배 좋게 튜닝하는 것보다, 도로를 직렬 고속도로로 포장(PCIe)하고 교통 경찰(버스 중재기)을 스마트하게 세우는 것이 컴퓨터 전체의 체감 속도를 올리는 숨겨진 비밀입니다.

---

## Ⅴ. 기대효과 및 결론

### 기대효과 및 미래 전망
- **플러그 앤 플레이 생태계 완성**: PCI, USB 같은 표준화된 버스 인터페이스의 통일 덕분에, 삼성 CPU, 엔비디아 GPU, 소니 메모리 등 전 세계 어떤 기업이 부품을 만들든 슬롯에 꽂기만 하면 하나로 동작하는 거대한 PC 모듈화 생태계가 완성되었다.
- **칩렛(Chiplet) 구조와 인터포저 버스**: 현대 무어의 법칙 한계에 따라, CPU 하나를 통짜로 만들지 않고 작은 조각(Chiplet)으로 만들어 기판 위에 이어 붙이는 기술이 대세다. 이 조각들 사이를 빛의 속도로 연결하는 UCIe (Universal Chiplet Interconnect Express) 같은 초미세 마이크로 버스 기술이 미래 반도체 패권을 쥐는 핵심 인프라로 떠올랐다.

### 결론
시스템 버스(System Bus)는 개별적으로 존재하던 연산, 제어, 기억, 입출력이라는 차가운 쇳덩어리 섬들을 하나의 유기적인 생명체(컴퓨터)로 결합해 준 신경망이자 혈관이다. 선을 늘리는 병렬 방식의 물리적 한계를 겪고 직렬 전송으로 패러다임을 바꾼 버스의 역사는, 폰 노이만 병목을 우회하기 위한 인류 하드웨어 엔지니어링의 치열한 투쟁기 그 자체다.

- **📢 섹션 요약 비유**: 사람의 심장(CPU)과 팔다리(I/O)가 제아무리 튼튼해도, 그 사이를 잇는 척수 신경망(시스템 버스)이 끊어지면 아무것도 할 수 없듯, 버스는 컴퓨터라는 거인의 핏줄이자 정보의 대동맥입니다.

---

## 📌 관련 개념 맵 (Knowledge Graph)

| 개념 명칭 | 관계 및 시너지 설명 |
|:---|:---|
| **폰 노이만 병목 (Von Neumann Bottleneck)** | CPU 속도와 버스/메모리 전송 속도의 격차로 인해 시스템 전체의 체감 성능이 버스 대역폭에 종속되는 현상. |
| **DMA (Direct Memory Access)** | 입출력 장치가 시스템 버스의 제어권을 CPU 대신 부여받아, CPU 간섭 없이 메모리에 직접 데이터를 쏘아주는 효율 극대화 기술. |
| **PCIe (Peripheral Component Interconnect Express)** | 과거 병렬 버스의 한계를 깬 고속 직렬 점대점(Point-to-Point) 버스 구조로 현대 그래픽카드와 NVMe SSD의 통신 표준. |
| **하버드 아키텍처 (Harvard Architecture)** | 명령어와 데이터를 단일 버스로 전송하던 체계를 깨고 버스 노선을 두 개로 분리해 정체를 해소한 아키텍처. |
| **버스 중재기 (Bus Arbiter)** | 여러 버스 마스터(CPU, DMA 등)가 동시에 공용 버스를 사용하려 할 때 신호등 역할을 하여 데이터 충돌(Collision)을 방지하는 하드웨어 제어기. |

---

## 👶 어린이를 위한 3줄 비유 설명
1. 컴퓨터 안에는 두뇌(CPU)와 창고(메모리), 스피커(입출력) 등 여러 친구들이 모여 살아요. 이 친구들이 물건을 주고받으려면 길이 필요하죠.
2. 각자 집 사이에 1:1로 길을 뚫으면 골목길이 너무 꼬여버려서, 도시 한가운데 아주 크고 넓은 **'공용 고속도로'** 딱 하나만 지었어요. 이걸 '시스템 버스'라고 해요.
3. 이 버스(도로) 덕분에 주소와 짐을 실은 트럭들이 서로 충돌하지 않고 착착 배달할 수 있어서 컴퓨터가 똑똑하게 돌아가는 거랍니다!
"""

c128 = """+++
title = "폰 노이만 병목현상 (Von Neumann Bottleneck)"
weight = 128
+++

# 폰 노이만 병목현상 (Von Neumann Bottleneck)

## 핵심 인사이트 (3줄 요약)
> 1. **본질**: 폰 노이만 병목현상 (Von Neumann Bottleneck)은 CPU의 폭발적인 연산 속도를, 데이터를 가져오는 공용 통로(시스템 버스)와 메모리의 느린 접근 속도가 따라가지 못해 시스템 전체 성능이 하향 평준화되는 구조적 정체 현상이다.
> 2. **가치**: 프로그램 내장 방식(명령어와 데이터가 단일 버스를 공유함)이 낳은 피할 수 없는 부작용이자, 지난 50년간 반도체 설계자들에게 캐시(Cache), 파이프라이닝, 분기 예측 같은 현대 프로세서 기술 혁신을 강제한 발전의 기폭제가 되었다.
> 3. **융합**: 이 병목은 단순히 하드웨어의 문제가 아니라, 클라우드 서버의 데이터 로딩 시간, AI 모델의 학습 지연(Memory Wall) 등 오늘날 대규모 분산 컴퓨팅과 소프트웨어 최적화에서 가장 먼저 고려해야 할 원초적 물리 법칙이다.

---

## Ⅰ. 개요 및 필요성 (Context & Necessity)

- **개념**: 폰 노이만 아키텍처는 프로그램 명령어와 데이터를 하나의 기억장치(메모리)에 담고 단일 버스를 통해 CPU로 가져온다. 이때 CPU의 처리 속도(수 나노초)에 비해 메모리 버스의 전송 속도(수십~수백 나노초)가 압도적으로 느려, CPU가 데이터를 기다리며 멍하니 노는 대기(Stall) 상태가 발생하는데 이를 폰 노이만 병목이라 한다.
- **필요성**: 무어의 법칙에 따라 CPU의 트랜지스터 집적도와 클럭 속도는 매년 60%씩 미친 듯이 향상되었다. 반면, 메모리(DRAM)의 발전은 주로 '용량 증가'에 치중되어 속도 향상은 연 7%에 불과했다. 이 '속도 격차(Processor-Memory Performance Gap)'가 점점 벌어지면서, 아무리 비싼 CPU를 달아도 데이터가 도착하지 않으면 연산 속도가 0이 되는 거대한 장벽(Memory Wall)이 도래했다. 이 병목의 원리를 이해해야만 시스템 최적화의 타겟을 잡을 수 있다.
- **💡 비유**: 주방장(CPU)은 세계 최고라서 1초에 양파 100개를 썰 수 있는데, 재료를 꺼내오는 단 하나뿐인 창고 문(버스)이 너무 좁고 조수(메모리)가 느려서 1분에 양파를 1개씩만 가져다줍니다. 결국 천재 주방장은 하루 종일 양파를 기다리며 담배만 피우게 되는 끔찍한 비효율입니다.

- **등장 배경**:
  1970년대 후반 튜링상 수상자 존 백커스(John Backus)가 수상 연설에서 이 단어를 처음 주창했다. 그는 단어 하나하나를 메모리에서 꺼내와야 하는 폰 노이만 구조의 "답답한 튜브(Tube)"를 비판하며, 이 병목을 피하려면 코딩 패러다임 자체(함수형 프로그래밍 등)를 바꿔야 한다고 역설했다.

```text
┌────────────────────────────────────────────────────────┐
│         폰 노이만 병목 (Von Neumann Bottleneck) 발생 메커니즘      │
├────────────────────────────────────────────────────────┤
│                                                        │
│   ┌─────────────────────┐                              │
│   │ CPU (Core)          │ 속도: 0.1ns (초고속)           │
│   │ ┌─────────┐┌────────┐│ 처리량은 무한정에 가까움         │
│   │ │ 제어 유닛 ││ 연산 유닛││                              │
│   │ └────┬────┘└────┬───┘│                              │
│   └──────┼──────────┼──────┘                              │
│            │ 대기(Stall)│ ◀── ⚠ CPU는 연산할 것이 없어 논다!     │
│   =========│==========│============================    │
│            ▼          ▼                                │
│        [ 병목 구역: 단일 시스템 버스 (System Bus) ]             │
│            │          │    속도: 통로가 좁아 한 번에 하나씩만 전송│
│   =========│==========│============================    │
│            ▼          ▼                                │
│   ┌─────────────────────┐                              │
│   │ Memory (명령어+데이터) │ 속도: 10ns ~ 50ns (매우 느림)    │
│   └─────────────────────┘                              │
│                                                        │
│  * 근본 원인: 명령어(코드)를 읽는 동작과 데이터(변수)를 읽는 동작이 │
│              서로 같은 길(버스)을 뺏기 위해 싸운다(구조적 해저드). │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** 그림 중앙의 '단일 시스템 버스'가 폰 노이만 병목의 핵심 현장이다. CPU는 `a = b + c`를 계산하기 위해 1. 명령어(`ADD`)를 버스로 가져오고, 2. `b`의 값을 버스로 가져오고, 3. `c`의 값을 버스로 가져온 뒤, 4. 그 결과 `a`를 다시 버스를 태워 메모리로 보내야 한다. 계산 자체는 1클럭(0.1ns) 만에 끝났지만, 네 번이나 좁은 버스 튜브를 오가느라 총 수백 ns가 소모되었다. 이것이 백커스가 지적한 "폰 노이만 튜브"의 덫이다.

- **📢 섹션 요약 비유**: 수백 마력의 엔진을 단 페라리(CPU)를 만들었지만, 출퇴근 시간이면 어김없이 꽉 막히는 1차선 올림픽대로(버스) 위에 차를 올려두었기 때문에 페라리나 경차나 도착 시간이 똑같아지는 슬픈 현상입니다.

---

## Ⅱ. 아키텍처 및 핵심 원리 (Deep Dive)

### 병목 현상을 유발하는 3대 물리적/구조적 요인

1. **공유 버스 구조의 한계 (Structural Hazard)**: 명령어 인출 단계(Fetch)와 데이터 접근 단계(Memory Access)가 단일 채널(버스)을 경쟁하므로 동시에 일어날 수 없다.
2. **프로세서-메모리 속도 격차 (Performance Gap)**: 논리 게이트(SRAM/레지스터) 기반의 CPU는 매년 기하급수적으로 빨라졌으나, 축전기(Capacitor)를 충/방전해야 하는 DRAM의 근본적 물리 한계로 접근 지연 시간(Latency)은 좁혀지지 않았다.
3. **오프칩(Off-chip) 통신 페널티**: CPU 다이(Die) 바깥에 있는 메인 메모리까지 전기 신호가 핀(Pin)과 메인보드 구리 배선을 타고 이동해야 하므로, 물리적인 거리(cm 단위)에 의한 빛의 속도 지연 및 커패시턴스(전기용량) 충전 시간이 강제된다.

### 현대 아키텍처의 폰 노이만 병목 돌파 전략 (Memory Hierarchy)

이 병목을 부수기 위해 하드웨어 공학자들은 "모든 데이터를 메모리에서 바로 가져오지 말고, 버스를 타기 전 **CPU 내부에 임시 보관 창고(Cache)**를 만들자"는 지역성(Locality) 기반 계층 구조를 창안했다.

```text
┌────────────────────────────────────────────────────────┐
│           병목 우회를 위한 메모리 계층 구조 (Memory Hierarchy)     │
├────────────────────────────────────────────────────────┤
│                                                        │
│                    [CPU 칩 내부]                        │
│             ┌─────────────────────────┐                │
│   레지스터    │          ALU            │   0.X ns       │
│   (가장빠름)  └───┬─────────────────┬───┘                │
│                 ▼                 ▼                    │
│   L1 캐시    ┌───────┐         ┌───────┐               │
│  (하버드구조) │ L1 Inst │ (분리)  │L1 Data│   ~ 1 ns       │
│             └───────┘         └───────┘               │
│                 ▼                 ▼                    │
│   L2/L3 캐시  ┌─────────────────────────┐               │
│             │   통합 L2 / L3 Cache    │   ~ 5 ns       │
│             └─────────────┬───────────┘                │
│  =========================│=========================   │
│                 폰 노이만 병목 지점 (시스템 버스)             │
│  =========================│=========================   │
│             ┌─────────────▼───────────┐                │
│   메인 메모리 │ Main Memory (DDR DRAM)  │  ~ 50 ns       │
│   (느리고 큼) └─────────────────────────┘                │
└────────────────────────────────────────────────────────┘
```

**[다이어그램 해설]** CPU 다이(Die) 밖으로 나가는 순간 지옥의 트래픽(폰 노이만 병목)이 시작된다. 그래서 현대의 프로세서는 칩 바깥으로 나가는 빈도를 줄이기 위해 내부에 캐시(Cache)라는 방파제를 겹겹이 쌓았다. L1 캐시는 명령어(Inst)와 데이터(Data)를 아예 물리적으로 쪼개어 하버드 아키텍처를 흉내 내 병렬 접근을 가능하게 했다. 최근 사용된 데이터의 95% 이상을 이 캐시 계층에서 막아냄(Cache Hit)으로써, 느려터진 메인 메모리(DRAM)까지 내려가서 버스 정체를 겪어야 하는 상황(Cache Miss) 자체를 없애버린 것이 현대 CPU 성능 방어의 핵심 원리다.

- **📢 섹션 요약 비유**: 매번 요리할 때마다 트럭을 몰고 대형 마트(메인 메모리)에 다녀오는 대신, 주방 바로 옆에 미니 냉장고(캐시)를 두고 자주 쓰는 대파와 마늘을 미리 쟁여두어 요리 속도를 비약적으로 끌어올린 전략입니다.

---

## Ⅲ. 융합 비교 및 다각도 분석

### 병목 해결 접근법 1: 하드웨어 진화 (Von Neumann -> Harvard)

| 해결 기법 | 적용 원리 | 효과 및 한계 |
|:---|:---|:---|
| **캐시 메모리 (Cache)** | 시간적/공간적 지역성 활용, 빠른 임시 메모리 배치 | 평균 지연 시간 급감. 단, 캐시 미스 시 페널티 증가 |
| **모디파이드 하버드 구조** | L1 영역에서 명령어 버스와 데이터 버스 물리적 분리 | 구조적 해저드 원천 차단. 칩 외부까지 분리는 비용 한계로 불가 |
| **파이프라이닝 (Pipelining)** | 이전 명령어가 데이터 읽을 때 다음 명령어 인출 | 명령어 스루풋(Throughput) 증가. 단, 메모리 지연 시 파이프라인 전체 스톨 발생 |

### 병목 해결 접근법 2: 대역폭(Bandwidth) 자체의 물리적 확장

버스를 분리할 수 없다면, 버스의 폭을 극단적으로 넓혀 '속도' 대신 한 번에 나르는 '물량'으로 승부한다.

- **멀티채널 메모리 (Dual/Quad Channel)**: DDR 메모리를 1개가 아닌 2개, 4개씩 병렬로 꽂아 버스 폭을 64비트에서 128비트, 256비트로 넓혀 데이터 전송량을 배가시킴.
- **HBM (High Bandwidth Memory)**: 칩 외부에 있던 메모리를 3D 실리콘 관통 전극(TSV)으로 층층이 쌓아 GPU 칩 바로 옆(인터포저)에 찰싹 붙여버림. 버스 폭을 무려 4096비트 이상으로 뚫어 오프칩 딜레이를 소멸시킨 AI 혁명의 일등 공신.

- **📢 섹션 요약 비유**: 도로가 막히는 문제를 풀기 위해 ① 출퇴근 시간을 겹치지 않게 분산시키거나(파이프라이닝), ② 아예 도로를 1차선에서 16차선으로 대폭 확장해버리는(HBM, 멀티채널) 두 가지 방식을 융합하여 정면 돌파하고 있습니다.

---

## Ⅳ. 실무 적용 및 기술사적 판단

### 실무 시나리오 및 소프트웨어 최적화

1. **시나리오 — 대용량 배열의 2차원 순회 처리 (Data Locality)**: C/C++로 영상 처리 매트릭스 필터를 짤 때, 배열을 행(Row) 우선으로 읽느냐 열(Column) 우선으로 읽느냐에 따라 실행 속도가 10배 이상 차이 나는 현상 발견.
   - **의사결정**: 폰 노이만 병목을 최소화하려면 '공간적 지역성'을 극대화해야 한다. C언어 배열은 행(Row) 단위로 메모리에 연속 저장되므로, 이중 `for`문에서 외부 루프를 `row`로, 내부 루프를 `col`로 돌려 순차 접근해야 한다. 만약 `col` 단위로 건너뛰며 읽으면 매번 캐시 미스(Cache Miss)가 발생하여 CPU가 폰 노이만 병목 구간(메인 메모리 버스)에 끊임없이 빠지는 최악의 참사가 벌어진다.

2. **시나리오 — 초거대 언어 모델(LLM, 챗GPT 등)의 추론 속도 저하**: 그래픽카드(GPU)의 연산력(FLOPS)은 수천조 번인데, 정작 챗GPT가 단어를 뱉어내는 토큰 생성 속도가 답답하게 느린 병목 구간 타격.
   - **의사결정**: LLM 추론은 대표적인 **Memory-Bound (메모리 대역폭 제한)** 워크로드다. 계산 자체는 가벼우나, 100GB가 넘는 모델의 파라미터(가중치 데이터)를 메모리에서 GPU 코어로 퍼 올리는 폰 노이만 버스가 한계에 도달한 것이다. 실무자는 모델 양자화(Quantization: FP16 -> INT8)를 통해 데이터 부피를 반으로 줄여 좁은 버스를 덜 통과하도록 '데이터 다이어트' 구조로 최적화해야 한다.

3. **시나리오 — 데이터베이스 인메모리(In-Memory) 아키텍처 도입**: 디스크 기반의 RDBMS 장비에서 I/O 대기시간이 임계치를 넘어 마이크로서비스 전체 API가 타임아웃 나는 상황.
   - **의사결정**: 가장 최악의 폰 노이만 병목은 디스크(Storage)로 향하는 I/O 버스에서 발생한다. 이를 끊어내기 위해 Redis나 Memcached 같은 인메모리 데이터베이스를 캐시 레이어로 전면 배치하여, 디스크 탐색 지연을 나노초 단위의 RAM 접근으로 끌어올리는 시스템 아키텍처 대수술을 감행한다.

### 안티패턴
- **Linked List 기반의 대용량 데이터 잦은 탐색**: 노드들이 메모리 공간 여기저기에 파편화(Fragmentation)되어 흩어져 있는 Linked List를 순회하면, CPU는 다음 주소로 건너뛸 때마다 캐시를 활용하지 못하고 무조건 메인 메모리까지 내려가야 한다. 연산 집약적인 실시간 게임 물리 엔진 등에서 `std::list` 대신 연속된 메모리 덩어리인 데이터 지향 설계(Data-Oriented Design)와 `std::vector`를 써야 하는 근본 이유가 바로 이 병목을 피하기 위함이다.

- **📢 섹션 요약 비유**: 천재 작업자(CPU)에게 일을 맡길 때, 서류가 이 방 저 방에 흩어져 있으면(Linked List) 작업자는 하루 종일 복도(버스)만 뛰어다니다가 쓰러집니다. 관련된 서류를 책상 위에 뭉텅이로 예쁘게 정렬해 주는(Vector, 지역성) 것이 진정한 소프트웨어 최적화입니다.

---

## Ⅴ. 기대효과 및 결론

### 한계 돌파를 위한 미래 기술: PIM (Processing-In-Memory)
폰 노이만 병목을 근본적으로 소멸시키는 미래 아키텍처가 부상하고 있다. 바로 **PIM (Processing-In-Memory)** 기술이다.
- "CPU가 메모리에 데이터를 가지러 가는 것"이 기존 폰 노이만 방식이라면, PIM은 **"메모리 칩 내부에 작은 ALU(연산기)를 집어넣어, 데이터가 있는 그 자리에서 직접 계산해버리자"**는 발상의 대전환이다. 이를 통해 거대한 시스템 버스를 데이터가 통과할 필요 자체가 사라지며, AI 반도체 시대의 에너지 소모와 지연 시간을 수십 배 단축할 궁극의 게임 체인저로 삼성이 주도하고 있다.

### 결론
폰 노이만 병목현상은 인류가 프로그램과 데이터를 하나의 논리적 공간에 압축하면서 치러야 했던 필연적인 세금이었다. 그러나 이 족쇄를 풀기 위해 엔지니어들이 치열하게 고민한 결과, 캐시, 파이프라인, 비순차 실행, 멀티스레딩 등 눈부신 현대 컴퓨터 구조의 진화가 촉발되었다. 병목은 고통이었지만, 역설적으로 컴퓨터 공학을 가장 비약적으로 도약시킨 원동력이었다.

- **📢 섹션 요약 비유**: 폰 노이만 병목은 인류가 컴퓨터라는 도시를 세울 때 생긴 고질적인 '출퇴근 교통 체증'입니다. 비록 짜증 나지만, 이 교통 체증을 풀기 위해 지하철(캐시), 고가도로(하버드 분리), 재택근무(PIM) 같은 혁신적 인프라가 발전하게 된 것입니다.

---

## 📌 관련 개념 맵 (Knowledge Graph)

| 개념 명칭 | 관계 및 시너지 설명 |
|:---|:---|
| **프로그램 내장 방식 (Stored Program)** | 명령어와 데이터를 단일 메모리에 저장한다는 구조적 설계로 인해 폰 노이만 병목을 낳은 근본 원인. |
| **메모리 계층 구조 (Memory Hierarchy)** | 빠르고 비싼 캐시와 느리고 싼 램을 층층이 배치하여 버스 병목 지연을 통계학적으로 우회하는 방어 체계. |
| **공간적/시간적 지역성 (Locality)** | 캐시 메모리가 적중률을 높여 폰 노이만 병목을 피할 수 있게 해주는 소프트웨어의 경험적 참조 성질. |
| **메모리 월 (Memory Wall)** | CPU 연산 속도와 버스/메모리 속도 격차가 돌이킬 수 없이 벌어져 코어를 늘려도 성능이 오르지 않는 장벽. |
| **PIM (Processing-In-Memory)** | 연산기와 메모리 간의 데이터 이동(버스) 자체를 소멸시키기 위해 메모리 반도체 내부에서 덧셈/곱셈을 수행하는 탈-폰노이만 미래 아키텍처. |

---

## 👶 어린이를 위한 3줄 비유 설명
1. 세상에서 햄버거를 가장 빨리(1초에 100개) 만드는 요리사(CPU)가 있어요.
2. 그런데 빵과 고기를 꺼내오는 창고 문(시스템 버스)이 너무 좁아서, 한 번에 하나씩만 재료를 꺼내올 수 있어요.
3. 결국 천재 요리사라도 재료가 올 때까지 요리를 못 하고 멍하니 기다려야 하는데, 이 답답하게 막히는 현상을 '폰 노이만 병목현상'이라고 부른답니다!
"""

write_file('125_stored_program.md', c125)
write_file('126_harvard_architecture.md', c126)
write_file('127_system_bus.md', c127)
write_file('128_von_neumann_bottleneck.md', c128)

print("Files generated.")
